# 地底ステージ 仕様書（ぴよ氏の冒険 / Underground Stage）

**起案:** 2026-07-25（Ver.1.527 時点）／**更新:** 2026-07-25（Ver.1.537・1.3提出後）
**位置づけ:** 単体の目玉リリース（1.4想定・大工程）。
**正の仕様:** 本書。実装後の細部変更は HANDOFF/メモリの changelog に追記する。

## ✅ 1.3（Ver.1.526〜1.537）で先行して入ったもの（2026-07-25 両ストア提出済み）
- **闇のカカシ**（門番ボス・R6/R12/R18…）＝手続き描画・対空「藁の棘」つき。
- **アカバネ**（急降下する空中雑魚・R11〜）。
- **ボス周期の定数化**＝`BOSS_CYCLE_ROUNDS`（現在6）。これ1つで以下が自動追随する:
  - `bossKindForRound`（ローテ番号＝特別回を除いた通し番号）
  - `bossEncounter()`＝`ceil(round / BOSS_CYCLE_ROUNDS)`（そのボスの「何回目」）
  - ボスHPの増加起点＝`gameRound - BOSS_CYCLE_ROUNDS`（1周目は一律100）
  - `BOSS_FLYING_EDGE_ROUND`＝`BOSS_CYCLE_ROUNDS + 1`（ボス戦の空中雑魚の解禁）
- **BGM素材は配置＆タグ除去済み・未配線**（実装時にコメントを外すだけ）:
  - `sounds/underground.mp3` … 地底ステージBGM
  - `sounds/boss_underground.mp3` … 地底ボス（ラスボス格）BGM
  - `audio.js` に登録用コメント、`gameplay.js` の `STAGE_BGM_CYCLE` に追加手順を明記済み。

---

## 0. ユーザー決定事項（2026-07-25）

- **正式名称 = 闇のカカシ**（カタカナ表記＝カカシ。旧・漢字「案山子」は使わない）。
- **✅闇のカカシは 1.3（Ver.1.528）で実装済み** ＝地底ステージ本体に先行して門番ボス単体を先に出した。R6/R12/R18…に出現・手続き描画（立ち絵画像なし）。詳細は [[piyo-gameplay-backlog]]。本仕様書の残りは**地底ステージ本体＋専用ボス「闇の巫女」（1.4想定）**。
- **構造 = R6=カカシ / R7=地底**（下記「1. 出現サイクル」で確定）。
- **地底のボス = 専用の大物ボス**（ラスボス格の新規ボス「闇の巫女」的なもの・カカシとは別）。
- **時期 = 単体の目玉リリース**。まず本仕様書を固めてから実装に着手。
- 地底ステージは**ランダム生成でなく作り込み**（ハンドクラフト）でよい。
- 強制スクロールではない。ただし SMB 同様**左には戻れない**。
- ラウンド5終了後、スクロールが止まり土管に入るしかなくなる。落下したところから始まる。
- アカバネ（1.527・急降下雑魚）は「ボスが1周した以降」に顔ぶれを変える起点として調整する（地底導入=1.4時に DIVE_BIRD_ROUND を再調整）。

---

## 1. 出現サイクル（**7ラウンド周期**・R6=カカシゲート → R7=地底）

エンドレスなので剰余で定義する。`gameRound` はボス撃破で +1 される既存仕様（gameplay.js）をそのまま使う。

> **⚠ 2026-07-25 修正（絶対原則: 正しくループすること）**
> 初版の本書は「カカシ=`round%6===0` / 地底=`round%6===1`」と書いていたが、**これは誤り**。
> その定義だと2周目以降ローテがずれる（実測: 2周目が `カラス→タマゴ→大蛇→フクロウ→カカシ→地底→カラス`
> ＝**ニワトリが飛ばされ、周期も崩れる**）。地底を足すと1周は **5種ローテ＋カカシ＋地底＝7ラウンド**になるので、
> `BOSS_CYCLE_ROUNDS` を **6→7** に上げるのが正しい。ユーザーの「地底を足したら空中雑魚はR8から」という
> 指定とも一致する（`BOSS_FLYING_EDGE_ROUND = BOSS_CYCLE_ROUNDS + 1 = 8`）。

**実装（`BOSS_CYCLE_ROUNDS = 7` に変更するだけで下記が成立）**

| 条件 | 内容 | 例 | 状態 |
|---|---|---|---|
| `round % 7 === 6` | **闇のカカシ（門番ボス）** = 地底の直前ラウンド | R6, R13, R20… | ✅1.3実装済 |
| `round % 7 === 0` | **地底ステージ＋地底ボス** | R7, R14, R21… | ⬜1.4 |
| それ以外 | 通常ボスローテ `BOSS_KINDS`（rooster/hawk/egg/snake/owl） | R1–5, R8–12… | 既存 |

ローテ番号は「**特別回（カカシ・地底）を除いた通し番号**」で数える:
```js
var specials = Math.floor(round / 7) + Math.floor((round + 1) / 7); // 地底回 + カカシ回
var rotIdx   = round - specials;
return BOSS_KINDS[(rotIdx - 1) % BOSS_KINDS.length];
```

**実測（この定義なら毎周まったく同じ顔ぶれで回る）**
```
1周目 R1-R7 : ニワトリ カラス タマゴ 大蛇 フクロウ カカシ 地底
2周目 R8-R14: ニワトリ カラス タマゴ 大蛇 フクロウ カカシ 地底
3周目 R15-  : ニワトリ カラス タマゴ 大蛇 フクロウ カカシ 地底
```

- 「7ごとに、カカシで門を開け→次のラウンドで地底に潜る」という**7ラウンド周期のミニアーク**。
- カカシ・地底ボスは通常ローテ配列（`BOSS_KINDS`）に入れない（特別枠）。
- **✅決定①**: 毎周ゲート付きで統一。カカシは既に1.3で稼働（現在は6周期）。地底導入時に7周期へ移行する。
- ⚠ 移行時は `BOSS_CYCLE_ROUNDS` を7にすると、**カカシの出現がR6/R12/R18…→R6/R13/R20…へずれる**（既存プレイヤーの体感変化）。これは周期を正すための不可避な変更として受け入れる。

---

## 2. コアモデル =「拡張ボスアリーナ」（ここが設計の肝）

地底は**新しいゲームモードではなく、既存ボス戦の拡張**として実装する。これにより**ランキング不変条件を一切触らずに**成立する。

### 2.1 なぜ載るか（既存の裏取り）
- 距離は `gameState.distance = Math.floor(gameState.camera.x / 10)`（index.html:5405）だけで決まり、ランキングと `Lv = floor(距離/300)+1` は全部これ由来。**不可侵**（Firebase がルールで検証）。
- **ボス戦はすでにスクロールを止めている**：`setupBossArena` が `gameState.gameSpeed = 0` にする（gameplay.js）。`camera.x += gameSpeed`（index.html:5404）なので**ボス戦中は camera.x が進まない＝距離が凍る**。
- **ラウンドはボス撃破で `gameRound++`**（gameplay.js:2780・bossState.phase 5）。

→ つまり地底ステージ＝「1枚壁のアリーナ」を「作り込んだ複数画面のアリーナ」に拡張したもの。ラウンド前進は通常ボス戦と同じ挙動。⚠ただし**距離だけは凍結せず前進させる**（2.2の方針変更）＝オートスクロールを切って `camera.x` をプレイヤー追従で進める。

### 2.2 距離・ランキングの扱い（✅2026-07-25 ユーザー決定で**方針変更**）

> **旧案（凍結）は破棄。地底で進んだ分は距離に加算する。**
> ユーザー決定＝「プレイヤーは能動的に右に移動する。ステージの長さに沿った規定量だが、**その分は距離にも加算する**」。

**なぜこちらの方が良いか（実装上の裏取り）**
- 距離の単一情報源は `gameState.distance = Math.floor(gameState.camera.x / 10)`（index.html:5408）で、その直前に `camera.x += gameState.gameSpeed`（5407）があるだけ。
- 地底では**オートスクロールを切り（`gameSpeed=0`）、`camera.x` をプレイヤー追従で前進させる**。すると距離は**既存の式のまま自動で増える**＝ランキング計算に**特別扱いを一切足さない**。
- 凍結案の方がむしろ「地底だけ距離が進まない」という例外を増やしていた。**加算案は単一情報源を壊さない**。

**公平性・不正の観点**
- 地底は**強制入場・固定長・1周に1回だけ**（`gameRound` で進行管理・撃破しないと出られない）。→ **周回して距離を稼ぐことは構造的に不可能**。
- 全プレイヤーが同じ条件で同じ量（ステージ長ぶん）を得る＝ランキングの公平性は保たれる。
- 加算量は**ステージ長で決まる規定量**（1周目=15画面=12,300px=**1,230m**）。

**厳守事項**
- `distance` / `Lv = floor(距離/300)+1` の**式には一切手を入れない**。`camera.x` を素直に進めることで結果的に加算する。
- `camera.x` は**単調増加のみ**（左壁クランプ＝SMB式）。巻き戻すと距離が減り、ランキングの単調性が壊れる。
- ⚠**`resetPlayerPosition` のカメラ巻き戻しを地底でも無効化する**（index.html:6668 は現在 `!bossState.active && !tutorialState.active`）。地底で落下死するたびに200px戻ると**距離が減る**＋左壁クランプと喧嘩する。→ 条件に地底を追加。
- ラウンド前進：**闇の巫女を撃破したら `gameRound++`**（通常ボスと同じ経路）。地上復帰後の次の通常ボスは `bossDistanceFor(次round)` にゲートされる。地底で距離が増えている分、**復帰直後に既に次のボス距離を超えていないか**を確認すること（超えていれば即ボスになる＝許容だが実測しておく）。

### 2.3 再利用する既存の仕組み
- `pipeRoomState` + `enterPipeRoom`（gameplay.js:437〜）＝**サブステートへのワープ／save/restore／出入り演出**のパターン。地底はこの「大型版」。
- `setupBossArena`＝スクロール凍結・壁・エンティティクリアの雛形。
- ボス撃破→`gameRound++`→通常地形の再開ポイント設定（`lastTerrainX`）の一連（gameplay.js phase5）。

---

## 3. 入場フロー（R7トリガー時）

> **⚠2026-07-25 修正（ユーザー指定・Ver.1.552）**: 初版のここは「通常ランを `bossDistanceFor(7)` の距離まで進む」と
> 書いていたが**誤り**。正しくは「**闇のカカシ＝門番を倒した時点で門が開く＝その場に土管が出る**」。
> R7に地上区間は無い（走らせない）。1.551まで初版どおり実装しており、撃破地点(12,000m)から土管(14,400m)まで
> **2,400m＝実測113秒**走らされていた。`checkBossTrigger` の地底分岐を距離条件の外へ出して即時配置に修正済み。

1. R6の闇のカカシを撃破 → 撃破演出 → 「ROUND 7」表示 → `gameRound=7`。
2. **スクロールが再開した瞬間、目の前に強制土管がせり出す**（距離条件なし）。スクロールを止め（`gameSpeed→0`）、
   画面の55%位置に大きな縦土管を配置。ぴよ氏はそれ以上進めず、**土管に入る以外の選択肢がない**（左壁で戻れない）。
   ヒント表示「▼ ちていへ もぐる」。⚠止まった画面内は必ず平地にする（穴があると落ちて詰む＝`placeUndergroundPipe` が床を保証）。
4. 土管に入る演出（既存のクリップ方式・穴に沈む）→ **暗転→地底へ**。
5. 地底の**天井の穴から落下してくる導入演出**（縦方向・無敵・入力ロック）。着地したら操作開始＝ここがR7の本体。

- ✅**決定②**: 強制土管は既存 `item_pipe.png` の拡大流用（初版）。

---

## 4. カメラ・操作

- **オートスクロール解除**：地底ではプレイヤー追従カメラ（`camera.x` はプレイヤー基準で算出）。
- **左壁クランプ（SMB式）**：`camera.x` は単調増加のみ。プレイヤーは到達済み最左より左へ戻れない（`player.x >= cameraLeftWall`）。
- 操作は既存のまま（左/右/ジャンプ/下スワイプ）。**追加操作は増やさない**（片手・デッドゾーン安全設計を維持）。
- ✅**決定③**: 追従カメラは**横主体＋軽い縦**（ユーザー決定）。

---

## 5. ステージ設計（✅1周目の仕様確定・2026-07-25）

**方針＝スーパーマリオブラザーズの「城ステージ」**。ユーザー決定により、**それなりの長さと難易度・ダメージギミックあり**で作る。
（初版メモにあった「精密プラットフォーミングにしない」は撤回。ただし片手操作なので**リズムが読める配置**にすること。）

### 5.1 1周目（R7）の確定値

**長さの根拠（実測ベース・2026-07-25）**: 歩行速度は `MOVE_SPEED=6px/f × 60fps = 360px/秒 = 36m/秒`（地底はオートスクロールなし＝自分の足だけ）。
15画面＝素走り34秒・**実プレイ約1.4分**（ギミック待ち/ジャンプ/被弾復帰込みで素走りの2.5倍想定）・**ボス戦込みで約2.4分**。
⚠**制約はランキングではなく反復**だった: 地底由来の距離は上位プレイヤーでも全体の**4〜9%**で歪みは軽微。一方、地底は同じ作り込みレイアウトが
R7/R14/R21…と繰り返し出る（131km級で**7回**）。15画面なら 2.4分×7回≒17分で飽きの手前。30画面（通常ラウンド同等）は3.8分×7回≒27分で作業感が出るため不採用。

| 項目 | 値 | 備考 |
|---|---|---|
| 長さ | **15画面**（12,300px ＝ **1,230m**） | 距離に加算される規定量。通常ラウンド(2,400m)の約半分 |
| 進行 | プレイヤーが**能動的に右へ**進む（オートスクロールなし） | `gameSpeed=0`＋追従カメラ |
| 左方向 | **戻れない**（SMB式・左壁クランプ） | `camera.x` 単調増加 |
| 溶岩に落ちた時 | **穴と同じ扱い**（`fallDeath`＝ライフ1減って復帰） | 新規ロジック不要 |
| ボス | 闇の巫女（HP**200**） | 「7. 地底ボス」参照 |

### 5.2 ギミック（1周目に入れるもの・✅ユーザー選択）
| ギミック | 内容 | 実装 |
|---|---|---|
| **ファイアバー**（回転炎） | 支点から炎が伸びて回転。城ステージの代名詞 | 新規（角度で炎セグメントを配置・当たりは円弧上の点群） |
| **火の玉** | 溶岩から一定間隔で跳ね上がる | 新規（放物線・周期） |
| **溶岩の池** | 足場の下は溶岩。落下＝`fallDeath` | 新規（見た目＋落下判定は既存の穴と同じ） |
| **トゲ床** | 静的なトゲ。接触ダメージ | 新規（最も軽い・通路を狭める用） |
| **上下に動く床** | 既存の `special:'moving'` を流用 | ✅既存（実装不要） |

- ⚠ ダメージは既存の `takeDamage()`（無敵3秒＝`INVINCIBLE_FRAMES 180`）を使う。**新しい即死は増やさない**（溶岩＝穴と同じ`fallDeath`）。
- ⚠ **リズムが読める配置**にする（ファイアバー・火の玉とも周期が一定＝待てば必ず抜けられる）。理不尽な初見殺しにしない。
- ジャンプ到達は**約183px**（v=16/g=0.7の実測値）。足場の段差はこれを超えないこと。

### 5.3 復帰（チェックポイント）
- 地底で `fallDeath` した時、既存の `resetPlayerPosition` は「カメラ+150から平地を探して上から落とす」＋**カメラを200px巻き戻す**。
- ⚠ 地底では**巻き戻しを無効化**（距離が減る＋左壁クランプと矛盾）。
- **直近の安全な足場**を記録しておき、そこへ復帰させる（チェックポイント方式）。溶岩の上に復帰させない。

### 5.4 反復対策・2巡目以降
- 1周目のレイアウトは**1種**（作り込み固定）。**難度は周回スケールで作る**（`bossEncounter()` 連動）。
- **2巡目以降の強化は別途決定**（ユーザー方針＝「2巡目以降はより難易度が上がっていく」）。想定ノブ＝ギミックの回転/発射速度・数、敵の速度と数、ボスHP。

### 5.5 地底タイルセット
暗い洞窟パレット。既存地形パレットのリカラー（`recolor` の tint 方式＝アカバネ1.527で実装済）か新規ドット。背景はBGM込みで「地下」感を出す。

### レベルデータ形式（たたき台）
```
UNDERGROUND_LEVELS = [ {
  widthPx: 12300,                     // 15画面
  platforms: [ {x,y,w,type}, ... ],   // 固定座標（ワールド原点=落下着地点）
  lava:      [ {x,w}, ... ],          // 溶岩の池（落下=fallDeath）
  fireBars:  [ {x,y,len,speed,dir}, ... ],
  fireballs: [ {x,y,period,power}, ... ],
  spikes:    [ {x,y,w}, ... ],
  enemies:   [ {x,y,kind}, ... ],     // 周回でステータス倍率
  checkpoints: [ x, ... ],            // 復帰位置
  bossArenaX: <奥のボス部屋の開始x>
} ]
```


---

## 6. 敵

- 地底内の雑魚は**ラウンドでステータススケール**（速度/数）。既存の `1+(round-1)*0.3` 倍率の考え方を流用。
- 地底専用の見た目替え（洞窟コウモリ等）を tint リカラーで用意してもよい（アート軽い）。
- **アカバネ（1.527）の再調整**：現在 R11起点。「ボスが1周した以降」＝5ボス一巡＝**R6以降**が本来の意図。地底導入に合わせ、`DIVE_BIRD_ROUND` を見直す（地上の通常ランでの顔ぶれ変化の起点）。地底内に出すかは任意（初版は地上のみでよい）。

---

## 7. 地底ボス ＝ **人型の術者**（✅2026-07-25 ユーザー決定）

- **ラスボス格の専用ボス**。通常ボス（rooster/hawk/egg/snake/owl）とは別枠・別サイズ・別演出。
- **モチーフ＝人型の巫女／魔女**（ユーザー選択）。既存6体は全部「鳥・生き物・物」なので、**人型は一目で格上と分かる**のが採用理由。
- ⚠**命名の注意**: 「魔女」は**プレイヤースキン「魔女ぴよ」と競合**するため、ボス名には使わない。`kind` は `priestess`（内部ID）を想定。
- ⚠**緑の丸型はNG**（ハロ回避・きぐるみ1.497の教訓）。配色は黒×紫＋発光（既存の闇テーマに合わせる）。
- ⚠**9+レーティング維持**: 流血・過度な人体表現はしない。顔は影＋光る目で表情を出す（カカシと同系統＝シリーズの統一感）。

### 7.1 見た目（たたき台）
- ぴよ氏と同じ**ちび頭身の人型**・黒×紫の装束・大きなフード／烏帽子・**顔は影で目だけ光る**。
- **常に浮遊**（地に足をつけない＝簡単には踏めない＝格上感）。サイズは通常ボス128pxより大きめ（例160px）。

### 7.2 攻撃（3フェーズ・既存資産の流用を優先）
| フェーズ | 攻撃 | 回避 | 流用元 |
|---|---|---|---|
| P1（HP100〜60%） | **呪弾**＝扇状に魔法弾3〜5発 | 隙間を抜ける／ジャンプ | `bossState.eggs` + `isFlame`（大蛇の毒と同機構） |
| P1〜 | **詠唱**＝高度を下げて硬直 | ＝**踏みチャンス**（`exposed`） | egg/snake の露出窓 |
| P2（60〜30%） | **瞬間移動**＝消えて別位置へ（紫の残光で予告） | 位置を追う | 新規（描画のみ） |
| P2〜 | **魔法陣**＝床に円の予告→光柱が立つ | 横移動で円から出る | カカシの帯予告と同思想 |
| P3（30〜0%） | **分身**＝3体に分かれ本物だけが撃つ | 見分けて本物を踏む | 新規（立ち絵の複製描画） |
| P3 | **大詠唱**＝画面が暗転し安全地帯が1箇所だけ光る | 光る場所へ走る | フクロウの `drawOwlDarkness` を流用 |

- **⚠カカシの教訓を最初から織り込む**: 踏んだ後は**必ず短い反撃**を入れ、頭上に居座り続けて踏み続けるだけで勝てないようにする（1.535で対空を後付けした反省）。
- **HP＝200（1周目・✅ユーザー決定）**。通常ボス100の2倍＝踏み20回相当（弾・必殺も入るので実際はもう少し短い）。2巡目以降は `bossEncounter()` 連動で増加（増分は2巡目の仕様決定時に確定）。

### 7.3 アート工程
[[piyo-sprite-motion-rule]] 厳守：**立ち絵=OpenAI / 動きの差分コマ=Veo動画からコマ切り出し**。**Veoは1本ずつ生成してユーザーに提示**（連続生成でクレジットを浪費しない）。
必要コマ（最小）: idle（浮遊）／詠唱（踏める姿勢）／被弾。分身はidleの複製で足りる。

---

## 8. 退場・報酬

**✅撃破報酬（2026-07-25ユーザー決定）＝ ゴールデンエッグ1個（毎回）＋ スコア倍額**
| 項目 | 通常ボス | 闇の巫女（地底） |
|---|---|---|
| スコア | 5,000（`BOSS_DEFEAT_SCORE`） | **10,000（倍額）** |
| コイン | `BOSS_COINS_ON_DEFEAT` | 多め（1.5〜2倍・調整ノブ） |
| ゴールデンエッグ | なし | **1個（毎回）** |

- エッグの希少性を守るため**1個に据え置く**（土管1%／日次1回という既存の希少化方針と整合）。上位プレイヤーは1ランでR7/R14/R21…と複数回到達しうるため、2個以上にすると供給過多になる。
- 付与は既存の撃破報酬ブロック（`bossState.defeatedTimer === 90`）に分岐を足す＝`gainScore` と `coins.push` の並びに `goldenEggs++` を追加。
- 闇の巫女撃破 → `gameRound++`（=8）→ 地上へ復帰。
- 地上へ復帰演出（土管から出る／せり上がる）→ 地底で進んだぶん加算された距離から通常ランを再開。
- 死亡：地底内で死んだ場合の扱い＝通常のゲームオーバー（復活導線は既存のまま）。**要確認⑦**: 地底を「死なない部屋」にはしない（ボス戦だから）。土管ボーナス部屋（死なない）とは別物。

---

## 9. ランキング不変条件（厳守・再掲）

- 距離 `floor(camera.x/10)` / 速度 / `Lv=floor(距離/300)+1` に**一切触れない**。
- 地底で進んだ分は距離に加算する（`camera.x` を素直に前進させるだけ＝式は無改修）。Firebase の score 書き込みロジック・ルール検証も無改修。
- 検証は「地底の前後で距離が**単調増加**していること（減らない）」「Lv が地底通過で飛躍しないこと」「地底の加算量がステージ長どおり（1,230m）であること」を実測する。

---

## 10. 新規実装チェックリスト

- [ ] **`BOSS_CYCLE_ROUNDS` を 6→7 に変更**（これ1つでローテ／遭遇回数／HP起点／空中雑魚解禁が自動追随）
- [ ] 出現サイクル判定（`round%7===0` を地底へ分岐）＋ローテ番号から特別回2種を除外
- [ ] 強制土管イベント（R7トリガー距離でスクロール停止・入場強制）
- [ ] 地底サブステート（save/restore・`pipeRoomState` 拡張 or 新 `undergroundState`）
- [ ] ⚠`resetPlayerPosition` のカメラ巻き戻しを地底でも無効化（距離が減るのを防ぐ）＋チェックポイント復帰
- [ ] 落下導入演出（天井穴→着地・無敵・入力ロック）
- [ ] プレイヤー追従カメラ＋左壁クランプ
- [ ] レベルデータ形式＋描画（複数画面の固定レイアウト）
- [ ] 地底タイルセット／背景／**BGM配線**（素材は配置済み＝`audio.js` のコメント2行を外す＋`stopAllBGM` 配列へ追加＋`STAGE_BGM_CYCLE` へ `'underground'` を追加して7周ループ化＋地底ボス戦で `boss_underground` を鳴らす）
- [ ] 闇の巫女（ボスロジック＋アート）
- [ ] 敵のラウンドスケール
- [ ] 退場演出＋報酬＋地上復帰（距離連続）
- [ ] 図鑑登録（地底ステージ／闇の巫女／地底雑魚）＋ i18n ja/en（登録漏れ防止ルール順守）
- [ ] アカバネの起点再調整

## 11. アート資産チェックリスト

- [ ] 闇の巫女（立ち絵OpenAI＋モーションVeo・1本ずつ確認）
- [ ] 地底タイル／背景（リカラー or 新規ドット）
- [ ] 強制土管/大穴（流用 or 新規）
- [ ] 地底雑魚の見た目替え（任意・tint リカラー）
- [ ] 地底BGM（[[piyo-suno-audio-tags]] 順守／OtoLogic 使用時はクレジット）

---

## 11.4 P2-b レベル構成の起案（2026-07-25・Ver.1.562時点）

### 世界観の三本立て（ユーザー指定）
| 出典 | 何を借りるか |
|---|---|
| **SMB 城ステージ** | 骨格。溶岩・ファイアバー・動く床・一本道の緊張・最後に「橋と斧」＝ボス |
| **SMB 地下ステージ** | 密度。閉じた天井、暗い、ブロック的なタイル |
| **Hollow Knight** | 空間。**縦に深い**、広間と狭い通路のリズム、廃墟の遺構、静けさ、届かない奥行き |

Hollow Knight らしさを出す具体策（既存の資産で足りるもの）:
① 縦カメラで2〜3画面ぶんの「広間」を作る ② 長い落下＝潜る感覚の核 ③ ギミックゼロの静寂部屋を挟む
④ `drawCaveBackdrop` の4層パララックスに**縦パララックス**を足して深さを出す ⑤ 石柱＝廃墟の遺構（実装済）
⑥ 背景に「行けない上層/下層」の輪郭を見せる

### 縦カメラ（`camera.y`）は**1行で入る**
世界描画は既に `ctx.save(); ctx.translate(-gameState.camera.x, 0);` … `ctx.restore();` で囲まれており、
地形・足場・敵・ボス・プレイヤー・エフェクトが全部この中にある（render.js の `render()`）。
→ **`translate(-camera.x, -camera.y)` にするだけ**で縦スクロールが成立する。物理はワールド座標なので無改修。
**距離は `camera.x` のみ由来なので縦は距離に一切影響しない**（ランキング不変条件に触れない）。

**⚠ただし縦カメラを入れる前に必ず直す2点（入れると即壊れる）**
1. **`cullByX(platforms, …)` が作り込みの足場を消す**（index.html `manageObjects`）。
   `[camera.x-200, camera.x+GAME_WIDTH+200]` の外を削除するので、**まだ到達していない前方の足場が消える**。
   `manageTerrain` には地底ガードがあるが `manageObjects` の cull には無い。→ 地底中は cull をスキップする。
   ⚠現状バレていないのは、地底にまだ platforms が1つも無いから。**P2-bで足場を置いた瞬間に露見する。**
2. **落下死とチェックポイントが画面座標基準**。
   `index.html:5783 if (player.y > GAME_HEIGHT + 100) fallDeath();` と
   `gameplay.js:242 player.y < GAME_HEIGHT`（チェックポイント記録の条件）。
   縦カメラで下へ降りると**降りただけで死ぬ／チェックポイントが記録されない**。→ 部屋ごとの死亡ラインをワールド座標で持つ。

### 構成案（7部屋・横16,000px）
部屋をHollow Knight風に「横の通路」と「縦の広間」で交互に組む。**縦は横の予算を消費しない**（距離が進まない）。

| # | 部屋 | 型 | 横px | 縦 | 内容 | 目安 |
|---|---|---|---|---|---|---|
| 1 | 落下の間 | 縦(降) | 600 | 2画面 | 天井の穴から長く落ちる＝「潜る」導入。ギミックなし | 8秒 |
| 2 | 石柱の回廊 | 横 | 3,000 | 1画面 | 歩くだけ。松明と柱。溶岩の池を1つだけ「見せる」（跳ばせない） | 17秒 |
| 3 | 熔炉の淵 | 横 | 3,500 | 1画面 | 溶岩を跳ぶ（2→3→4タイル）。火の玉が絡む | 20秒 |
| 4 | 崩れた尖塔 | 縦(登) | 800 | 3画面 | 動く床＋ファイアバーで上へ。**上がるほど背景の赤が抜ける＝安全になる報酬** | 25秒 |
| 5 | 王の廊下 | 横 | 4,000 | 1画面 | 尖塔の最上部。ファイアバー＋トゲ床の複合＝**最難関** | 22秒 |
| 6 | 深淵へ | 縦(降) | 800 | 3画面 | 一気に降りる。**降りるほど赤くなる**。落下＋足場 | 20秒 |
| 7 | 巫女の門 | 横 | 3,300 | 1画面 | ギミックゼロ。静寂と大きな扉。ボス前の溜め | 18秒 |
| | **計** | | **16,000px** | | | **約130秒** |

**設計の軸＝「登る＝報われる／降りる＝危険が増す」**。縦は距離が止まるので、横に走り続ける地上との対比で
**「立ち止まって考える場所」**として自然に機能する。序（部屋1-2）と終（部屋7）にギミックゼロ区間を置き、
落下直後の事故死とボス前の消耗を防ぐ。

### 作図の物理的な上限（実測値・必ず守ること）
| 項目 | 値 |
|---|---|
| 歩行 | 3.0px/f（`MOVE_SPEED × UG_SPEED_RATE`） |
| ジャンプ飛距離 | **135px**＝穴/溶岩は**最大4タイル(128px)** |
| ジャンプ最高到達 | **175px**＝段差は**実用150px**まで |
| 1画面 | 820×450px ＝ 25.6×14タイル（32px） |
| 地面 | `GROUND_Y = 348` |

### ✅決定: 1回あたりの所要時間は**このまま**（2026-07-25 ユーザー判断）
**16,000px / 800m / 1回 約4分 / 上位ラン(7回)で約28分** を採用する。
理由＝「マリオの城ステージもある程度時間がかかる。むしろ良い長さ」（ユーザー）。
⚠これは過去の「3.8分×7回≒27分は作業感が出るので不採用」（コミット009fe79）を**上書きする決定**。
地底は単体の目玉ステージであり、通常ラウンドの尺基準は当てはめない、という整理。**横を削る案は不採用。**

### ✅新規敵「骨だけの鳥」（2026-07-25 ユーザー発案・P2-bで実装）
地底にはボスだけでなく**専用の新規雑魚**を出す。既存の雑魚（`chick`/`golden_chick`/`mama_chick`/`flying_chick`/`dive_bird`）も出す。

- **立ち位置＝マリオの「カロン（Dry Bones）」**: 踏んでも倒れるだけで**骨がバラバラになって少し後に再生する**。
  つまり「一時的に無力化はできるが、根本的には避けて進む敵」。城ステージの緊張を作る定番の役割。
- **なぜ地底に合うか**: ①城/廃墟の世界観に直結 ②「倒せない敵」は Hollow Knight 的な圧にもなる
  ③既存の雑魚は全部「倒せる」ので、**倒せない敵は地底の固有性**になる。
- **⚠実装の勘所**:
  - 既存の踏み処理は「踏む＝撃破」。骨鳥は**踏み＝崩壊(collapse)→N秒後に再生(revive)** という状態を持つ別ロジックにする。
  - 崩壊中は当たり判定を消す（骨の山として描画）。再生時は軽い予兆を出す（理不尽な復活にしない）。
  - **図鑑登録は「撃破時」の規約**（1.474〜）。倒せない敵なので、**崩壊させた時点で登録**するか別枠にするかを決める必要あり。⬜未決定
  - スコア/コインは崩壊で入れるか（連続踏みで稼げてしまわないか）を要検討。⬜未決定
- **名前は未決定**⬜。既存の命名規則＝ボスは「闇の〇〇」（闇のカカシ/闇の巫女）、雑魚はカタカナ（アカバネ）。
  カタカナ2〜4文字の案: **ホネドリ / カラボネ / シャレコ**。ユーザー決定を待つ。

### ⚠P0（解決済み・記録として残す）: 1回あたりの所要時間
`加算レート9m/秒 × 横移動の時間 = 加算量` は動かせない。**800mを9m/秒で稼ぐには横移動だけで89秒が必須**。
そこへ縦の部屋（約53秒）とギミック待ち・被弾復帰（約50秒）が乗る。

| 案 | 横px / 加算量 | 1回の所要 | 上位ラン(7回)の合計 |
|---|---|---|---|
| このまま | 16,000px / 800m | 約4分（ボス込み） | **約28分** |
| **横を削る（推奨）** | 10,000px / **500m** | 約3分 | **約21分** |
| レートを上げる | 16,000px / 800m・13m/秒 | 約3分 | 約21分 |

⚠ユーザーは過去に「2.4分×7回≒17分は可・3.8分×7回≒27分は作業感が出るので不採用」と判断済み（コミット009fe79）。
**28分はその却下ラインを超える**ので、着手前に決めること。

---

## 11.5 実装の進捗（2026-07-25 時点）

| フェーズ | 状態 | 版 |
|---|---|---|
| **P1 基盤** | ✅**完了** | 1.541 |
| **P2-a タイル/背景** | ✅**完了** | 1.542 |
| **速さの確定（`UG_SPEED_RATE=0.5`）** | ✅**完了** | 1.543→1.544 |
| **入場土管・演出・SE・整合まわり** | ✅**完了** | 1.545〜1.562 |
| **P2-b 構成の起案** | ✅**完了**（[11.4] 参照） | 1.562 |
| **P2-b ギミック4種＋シャレコ＋縦カメラ＋レベル本実装** | ✅**完了**（[11.6] 参照） | **1.563** |
| **P2-b 調整（ユーザー報告8件）＋ボス闘技場の器** | ✅**完了**（[11.7] 参照） | **1.564-1.565** |
| **長さ 1,200m 化＋敵の倍増** | ✅**完了**（[11.8] 参照） | **1.566** |
| **怪しい老婆の店（外観／品揃え／店内）** | ✅**完了**（[11.9] 参照・⬜一枚絵2点だけ未納品） | **1.567-1.569** |
| P3 闇の巫女（本ボス・ロジック→アート） | ⬜**次はここ** | — |
| P3 闇の巫女（ロジック→アート） | ⬜ | — |
| P4 敵スケール・報酬・図鑑・調整 | ⬜ | — |

> ⚠**P2-b を描く前に必ず [11.4] の「作図の物理的な上限」を読むこと。** 地底の歩行は 3.0px/f＝
> **ジャンプ飛距離135px（32pxタイルで4枚ぶん）が穴・溶岩の池の上限**。地上(270px/8枚)の感覚で設計すると渡れない。

### ✅P1（Ver.1.541）で入ったもの
- `BOSS_CYCLE_ROUNDS` 6→**7**（1周＝5種ローテ＋カカシ＋地底）。`isUndergroundRound`/`isScarecrowRound` 新設。
  ローテは特別回2種を除いた通し番号で回す＝**毎周完全に同じ顔ぶれで反復**（実測済み）。
- `undergroundState`＋`enterUnderground`/`exitUnderground`/`updateUnderground`/`setupUndergroundStage`（gameplay.js 冒頭）。
  ループ配線は bootstrap.js の `updateUnderground()`。
- **距離加算**＝`camera.x` をプレイヤー追従で進めるだけ（式は無改修）。⚠端末差バグを修正済み：
  カメラ終端を「レベル幅−画面幅」にすると横長端末ほど加算が減る（実測 最大16m差）→ `camMaxX = originX + UG_TRAVEL_PX` 方式へ。
- ガード追加: `manageTerrain`／ランダム湧き／`checkPipeTrigger`／`checkShopTrigger`／`updateGameSpeed`（毎tick再計算で `gameSpeed` が戻るのを0固定）。
- 落下復帰: `resetPlayerPosition` に地底分岐（**カメラ巻き戻しを無効化**＝距離が減るのと左壁クランプの矛盾を防ぐ／チェックポイント復帰。
  ⚠**画面内で接地している時だけ**記録＝落下中のフレームを拾うと復帰先が画面外になる）。

### ✅P2-a（Ver.1.542）で入ったもの
- 洞窟タイル3種（sprites.js）: `terrain_cave_top`（砕けた岩肌）/`terrain_cave_dirt`/`terrain_cave_brick`（石積み）。
  ⚠32pxで横反復するので**規則的な点列を作らない**設計（初版は黄色い結晶が並んで安っぽく、作り直した）。
- `BIOME_CONFIGS[5] = underground`（洞窟パレット＋空グラデーション）。`updateBiome` で地底中は5に固定し `bgCache`/`terrainCache` を作り直す。
- `getTerrainCache` を地底対応＝**見た目だけ差し替え**（type は `ground`/`elevated` のままなので**物理は地上と完全に同一**）。キャッシュキーに地底フラグを含める。
- `drawCaveBackdrop`（render.js）＝4層パララックス＋**石柱の松明**（明滅・当たり判定なしの飾り）＋溶岩の照り返し（⚠地面より下は地形に覆われるので光は地面の**上**に置く）＋水滴。地底では雲を非表示。

### ✅距離加算の速さ（Ver.1.543・ユーザー報告対応）
- **計算結果**: 地上のスクロールは Lv17(4,800m)で頭打ち＝以降ずっと **6.0px/f = 36.0m/秒**。
  地底が始まる R7(14,400m) では地上も既に36.0m/秒なので、通常歩行(6px/f)なら**地底も36.0m/秒＝厳密には速すぎない**。
- **問題は2つ**: ①はやあし所持(1.3倍)だと 7.8px/f = **46.8m/秒＝地上では絶対に出せない速さ** ②自分で走る間ずっと最高速でカウンタが回る体感。
- **対策(1.543)**: カメラ前進を `BASE_SCROLL_SPEED*5.0*UG_DIST_RATE` で頭打ち＋上限超過時は画面右端でプレイヤーを止める。
  → **⚠この「カメラだけ絞る」方式は 1.544 で廃止**（下記）。`UG_DIST_RATE` はもう無い。

### ✅速さの一本化＝`UG_SPEED_RATE`（Ver.1.544・現行仕様）

ユーザー要望「加算をLv1相当(7.2m/秒)まで落とす／はやあしを地底で無効化する」を受けて再設計した。

**⚠カメラだけ絞ってはいけない（1.543方式の欠陥）**
歩行6px/f に対しカメラを1.2px/fにすると、プレイヤーは約1.8秒で画面右端のクランプに到達する。
クランプは `player.x` を止めるだけでなく **`velX = 0` する**ので、以後**前方へジャンプできない**＝
P2-b の溶岩の池もトゲ床も飛び越えられなくなる。1.543で表面化しなかったのは、r=1.0 では
カメラ上限(6px/f)＝歩行(6px/f)でクランプに触れなかったから。

**→ 歩行とカメラ前進の両方に同じ倍率を掛ける（`UG_SPEED_RATE` 1本）**
`MOVE_SPEED(6) === BASE_SCROLL_SPEED*5.0(6)`＝地上のスクロール上限 が偶然一致しているので、
同じ倍率を両方に掛ければ「歩行 = カメラ上限」が常に成立し、**右端クランプは純粋な保険に戻る**（貼り付きゼロ）。
はやあし(1.3倍)は `speedMul` の使用箇所で地底だけ上書き＝`gameState.speedMultiplier` 自体は触らないので地上復帰で自動に戻る。

**採用値 = `UG_SPEED_RATE = 0.5`**（歩行3.0px/f・素走りの踏破69秒）

### ✅「見かけ上のm」だけを半分に＝`UG_DIST_SCALE`（Ver.1.548・現行仕様）

ユーザー報告「人間が歩く速度にしては少し歩いただけで距離の加算が多すぎる」への対応。
要望は**「実際の移動速度・床や物との位置関係・物理はそのままで、数値上の加算だけ半分」**。

**⚠カメラを2本に分ける必要はない。** 距離の式の**入力側**を圧縮すれば足りる:
```js
// updateUnderground: カメラが d px 進むたび、その (1-UG_DIST_SCALE) 倍を積む
gameState.ugDistOffset += d * (1 - UG_DIST_SCALE);
// 距離: 式の形は不変。入力から圧縮量を引くだけ
gameState.distance = Math.floor((gameState.camera.x - gameState.ugDistOffset) / 10);
```
- **カメラ・歩行速度・地形/ギミックとの位置関係・当たり判定・踏破時間は完全に不変**（描画側は1行も触っていない）。
- **単調増加は保たれる**: offsetの増分 `d*0.5` はカメラの増分 `d` より必ず小さいので距離は必ず増える。
- **端末差なし**: カメラ走行は全端末で `UG_TRAVEL_PX` 固定＝減る量も 6,150px = 615m で一致。
- offsetは**ラン中ずっと残る**＝地上へ戻った後も「地底で稼いだ分が半分」の状態が続く（`resetGame` でリセット）。
- **地上の距離の増え方は完全に従来どおり**（地上では offset が増えない）。

**結果**: 加算レート 18.0m/秒 → **9.0m/秒**（歩行3.0px/f は不変）。

**⚠加算量は「`UG_TRAVEL_PX × UG_DIST_SCALE ÷ 10` (m)」で決まる。** 圧縮すると同じ長さでは加算量も半分になるため、
ユーザー指定の **800m** に合わせて `UG_TRAVEL_PX` を 12,300 → **16,000px（19.5画面・踏破約89秒）** に延長した（1.549）。

**距離を見る判定はすべて `gameState.distance`（＝見かけ上のm）を参照済み＝自動的に同期している**（1.549で全数確認）:
レベルアップ `floor(gameState.distance / SPEED_UP_INTERVAL)+1`／ノーダメージ継続ボーナス `gameState.distance - noDmgMark`／
ボス出現・おみせ・土管の抽選／バイオーム／必殺ゲージ／ゲームオーバーの `finalGameStats.distance`（＝ハイスコア判定・ランキング送信）／
実績の自己ベスト `as.bestDistance`。**`camera.x` から直接距離を出しているのは距離の式1箇所だけ**なので、取りこぼしは無い。

**実測（Ver.1.548・通し）**: 地底の加算レート **9.0m/秒**（カメラ前進3.000px/f・offset増分1.500px/f＝ちょうど半分）／
加算量 **615m**／踏破 **69.7秒（不変）**／画面内の位置 **291px（不変・貼り付きなし）**／距離が減った回数 **0**／
地上復帰後は 36m/秒に戻り offset は増えない／R7→R8。

**調整ノブ**: `UG_DIST_SCALE`（core-state.js）。1.0で圧縮なし（＝1.547以前の1,230m）、0.5が現行、0.25ならさらに半分。

### ✅バイオーム周期とボスの整合＝`stageShiftM`（Ver.1.553・現行仕様）

**⚠P1(1.541)からずっと壊れていた不変条件**: バイオームは `floor((距離 % 2400) / 600)` ＝**絶対距離の2,400m周期**、
ボスも**絶対距離の2,400m刻み**。この一致があるから「1ラウンド＝草原→砂漠→雪山→夜→ボス」が成立している。
**地底は2,400mの倍数でない量を足す**ので、そのままだと以降ずっとズレ続ける
（実測: R8が3,982m＝`砂漠→雪山→夜→草原→砂漠→雪山→夜`。1,230m/615m/800m のどの値でも同様にズレた）。

**対策**: `gameState.stageShiftM`（m）を新設し、**`getBiomeIndex` と `bossDistanceFor` の両方に同じ値を効かせる**。
```js
// 退場時（gameRound++ より前）にグリッドを引き直す
gameState.stageShiftM = gameState.distance - bossDistanceBaseFor(gameRound);
// バイオーム: floor(((距離 - stageShiftM) % 2400) / 600)
// ボス距離  : bossDistanceBaseFor(round) + stageShiftM
```
両者が同じ量ずれる＝**退場地点がちょうど草原の頭になり、次のボスは必ず2,400m先**になる。周回でも自動累積する。

**ユーザー決定（1.553）**: R1=草原→砂漠 / R2=雪山→夜 / **R3以降はずっと草原→砂漠→雪山→夜** / 地底 / 再び草原→…
→ 通常プレイでは `stageShiftM = 0` なので **R1〜R6の見た目は従来と完全に同一**（相対化の副作用なし）。

**検算（1〜3周目）**

| | 1周目(R7) | 2周目(R14) | 3周目(R21) |
|---|---|---|---|
| カカシ | 12,000m | 27,218m | 42,436m |
| 地底 入場→退場 | 12,018→12,818 | 27,236→28,036 | 42,454→43,254 |
| `stageShiftM` | −1,582 | −3,164 | −4,746 |
| 次のボス | 15,218m | 30,436m | 45,654m |
| **次ラウンドの長さ** | **2,400m** | **2,400m** | **2,400m** |
| 退場地点／ボス直前 | 草原／夜 | 草原／夜 | 草原／夜 |

**⚠カカシが12,000mちょうどでなく12,017mでアリーナが固まる理由**: `checkBossTrigger` は12,000mで発火するが、
「WARNING!」演出（`BOSS_WARNING_DURATION`=120フレーム）の間もスクロールが続き、`setupBossArena` で初めて
`gameSpeed=0` になるため。**120フレーム × 1.5px/f(安全地帯速度) ÷ 10 = 18m**。
**⚠これは全ボス共通**（R1は1,218m、R2は2,418m、R3は4,818m…で固定される）。通常ラウンドは次のボスが
絶対距離で決まるので累積せず表に出ないが、地底だけは補正値に乗って**以降のボス距離とバイオーム境界に永久に残る**。

**→ 補正値は「実測の退場距離」ではなく「設計値どおりの退場位置」から作る（1.555・ユーザー指定「端数を出さない」）**
```js
var ugAddM     = UG_TRAVEL_PX * UG_DIST_SCALE / 10;          // 地底の設計加算量 = 800m
var cleanExitM = bossDistanceFor(gameRound - 1) + ugAddM;    // カカシのボス距離(補正込み) + 800
gameState.stageShiftM = cleanExitM - bossDistanceBaseFor(gameRound);
```
これで補正値・ボス距離・バイオーム境界が**必ずキリの良い数**になる。プレイヤーが実際に稼いだ17〜18mは
距離としてそのまま残る（損得なし）＝**境界だけが整数に揃う**。
⚠ボス側の挙動（WARNING中もスクロールする）は**変えない**こと。ユーザー判断＝「表に出なければ十分」。

**検算（補正値・ボス距離・境界がすべてキリの良い数）**

| | 1周目(R7) | 2周目(R14) | 3周目(R21) |
|---|---|---|---|
| `stageShiftM` | **−1,600** | **−3,200** | **−4,800** |
| 次のボス | **15,200m** | **30,400m** | **45,600m** |
| バイオーム境界 | 12,800 / 13,400 / 14,000 / 14,600 | 28,000 / 28,600 / … | 43,200 / 43,800 / … |

### ✅ゴールデンエッグへの影響（1.553・確認済み）
一次抽選 **2,500m/30%**、救済抽選 **4,300m/80%**。どちらも地底の開始（12,018m）よりはるか手前で、
`ugDistOffset` は地底に入るまで0＝**判定距離も確率も一切変わらない**。R7には土管ボーナス部屋も出ないので
部屋経由の1/20抽選にも影響しない。

**⚠なぜ要望の 0.2（7.2m/秒）にしなかったか**（2つとも算術的な帰結で、実装の工夫では回避できない）
1. **時間**: `レート × 踏破時間 = 総加算量` なので、総量1,230mを固定したままレートを1/5にすると時間が5倍になる。
   0.2 は素走り171秒＝上位ラン(地底7回)で**約33分**となり、[9. 長さの根拠] で**すでに却下した30画面案(27分)より長い**。
2. **ジャンプ**: 滞空は `JUMP_FORCE=-16 / GRAVITY=0.7` で**45フレーム固定・最高到達175px**（実測）
   ＝飛距離は歩行速度にそのまま比例する。**P2-bの穴/溶岩の幅はこの表が上限**。
   | 歩行 | 飛距離 | 跳べる穴(32pxタイル) |
   |---|---|---|
   | 6.0px/f (1.0) | 270px | 8タイル |
   | **3.0px/f (0.5)** | **135px（実測）** | **4タイル** |
   | 2.0px/f (0.33) | 90px | 2タイル（設計が窮屈・実用下限） |
   | 1.2px/f (0.2) | 54px | 1タイル＝**ぴよ氏の幅48pxとほぼ同じ＝穴が作れない** |

- **調整ノブ**: `UG_SPEED_RATE`（core-state.js）。総加算量(1,230m)はレートに依らず不変＝**レートを変えても踏破時間だけが動く**。
  総加算量そのものを減らしたい場合は `UG_TRAVEL_PX` を下げる（ステージも横に短くなる）。
- **実測（Ver.1.544・はやあし所持のまま地底へ入って計測）**:
  歩行 velX = **3.00**（はやあし1.3倍が効いていれば7.8になる＝無効化を確認）／カメラ前進 = **3.000px/f = 18.0m/秒**
  ／カメラと歩行が完全一致し、プレイヤーは画面内 **290px（＝GAME_WIDTH×0.35）で固定**・右端クランプ748pxには一度も触れず
  ／踏破 **4,158フレーム = 69.3秒**（内訳: 立ち上がり58f＋12,300÷3.0=4,100f）／カメラ走行 **12,300px ＝ 加算 1,230m（設計値ぴったり）**
  ／距離が減った回数 **0**／R7→R8／地上復帰後は `speedMultiplier=1.3` が再び効く（velX が 3.0→4.56 と 7.8 に向けて再加速するのを確認）。
- 🔜P3: 「闇の巫女」戦だけ通常速度に戻したくなったら `speedMul` の条件に `!bossState.active` を足す（index.html にコメント済み）。

---

## 11.6 ✅P2-b 完了（Ver.1.563）

### レベルデータの表現形式（決定）
**1マス=32pxのテキストマップ**。`core-state.js` の `UG_LEVEL_ROOMS` が正。
⚠**行は必ず `ugRow(幅, 土台文字, [[開始列,'上書き文字列'], …])` で書く**（100文字の生リテラルを並べると編集不能になる）。

**縦の約束（これ1つで部屋どうしが自動でつながる）**
- 部屋の床面は必ず**マップ最下行の3行上**（下に2行の詰め物）。
- カメラ下限 `camMaxY = マップ下端 − GAME_HEIGHT`。
- この2つから床面は常に画面 y=354 に来る ＝ **隣の部屋と床の worldY を揃えるだけでカメラの高さも一致する**。

**凡例**: `#`岩(ソリッド) / `B`石積み(ソリッド) / **`b`石積みの飾り(当たり判定なし)** / `=`片道足場 / `M`動く床 /
`L`溶岩 / `^`トゲ / `F`(長4時計) `G`(長4反時計) `H`(長6時計) ファイアバー / `f`(周期150) `e`(周期100) 火の玉 /
`o`コイン / `S`シャレコ / `c`ひよこ `g`ゴールデン `m`ニワトリ `v`飛行 `d`アカバネ

### 実装した部屋（7部屋・520タイル＝16,640px）
| # | 部屋 | 幅(タイル) | 行 | topY | 床worldY | 型 |
|---|---|---|---|---|---|---|
| 1 | 落下の間 | 19 | 40 | −4 | 1180 | 縦(降) |
| 2 | 石柱の回廊 | 94 | 15 | 796 | 1180 | 横 |
| 3 | 熔炉の淵 | 109 | 15 | 796 | 1180 | 横 |
| 4 | 崩れた尖塔 | 32 | 57 | −548 | 底1180→頂−164 | 縦(登) |
| 5 | 王の廊下 | 125 | 15 | −548 | −164 | 横 |
| 6 | 深淵へ | 32 | 57 | −548 | 頂−164→底1180 | 縦(降) |
| 7 | 巫女の門 | 109 | 15 | 796 | 1180 | 横 |

### ⚠実装中に見つけて直したバグ（どれも「地底を作ると初めて露見する」類）
1. **`updatePlatforms` の地形クランプが天井にスナップする**（index.html・**既存バグ**）。動く床に乗った瞬間、
   横に重なる地形のうち**最初に見つけた1つ**へ問答無用で吸着していた。地上は地形が全部 GROUND_Y 以下なので
   偶然正しく動いていたが、地底は天井があるため**プレイヤーが天井の上へ飛ぶ**（実測 y=479→−596 を1フレームで）。
   → ①移動前の足元より上の地形は候補から外す ②候補のうち**一番高い床**を選ぶ、に修正。
2. **`cullByX(platforms,…)` が未到達の足場を消す** → 地底はチュートリアルと同じく右をカリングしない。
   ⚠敵は遅延スポーンなので従来どおり刈る（全部を最初に実体化すると到達前に自分で穴へ落ちる）。
3. **落下死とチェックポイントが画面座標基準** → 部屋ごとに `deathY`（ワールド座標）を持つ。
   `updateEnemyPhysics` の敵除去ライン（`GAME_HEIGHT+120`）も同様に修正（下の部屋の床にいる敵が即消えていた）。
4. **チェックポイントを動く床の上で記録すると無限死** → `ugOnStaticGround()` で静止した足場に限定。
5. **敵の速度に地上の倍率をそのまま使うとほぼ同速**（R7で敵2.2〜3.1px/f 対 プレイヤー3.0px/f）
   → `ugMakeEnemy` で `UG_SPEED_RATE` を掛けて地上と同じ相対速度に揃えた。
6. **溶岩を「見せる」つもりが石橋の下に完全に隠れていた** → 床の行を開けて裂け目を作り、その底に溶岩を置いた。
7. **門を `B`（ソリッド）で組むと床からまぐさまで192pxの壁になり越えられない** → `b`（飾り）を新設。
8. **深淵の右壁を全高ふさいでいて出口が無かった**（尖塔の左壁と対称の作りを忘れていた）。

### ✅実測（Ver.1.563・ボット通し・GAME_WIDTH=820）
落下の間→回廊→熔炉→尖塔→王の廊下→深淵→巫女の門を**全部踏破**／**5,997フレーム＝100秒**（ボットの最短寄り。
人間はギミック待ちと被弾でこれより長い）／**加算 12,000→12,800m ＝ ちょうど 800m（設計値）**／
**距離が減った回数 0**／R7→R8／`stageShiftM = −1,600`（[11.5] の検算表と一致）／
退場後 `camera.y=0` に復帰・バイオームは草原(0)・地底の配列は全部空／コンソール0。

**尖塔の各段の到達性は1段ずつ実測して全段 ✅**（1180→1052→…→−68→出口−164。段差は最大128px・
動く床も含めて到達可能）。**深淵の各段も落下先を1段ずつ実測して全段 ✅**（最下段は必ず安全な着地点の真上）。

### ⚠作図の実運用ルール（[11.4] の表からの変更点）
**必要な穴/溶岩は最大3タイル(96px)にする。** [11.4] は「135px＝4タイル(128px)まで」としていたが、
4タイルは余裕7pxで、最高速のまま端ギリギリを踏まないと落ちる＝片手操作では事故になる。3タイルなら余裕39px。
**トゲの連続も同じ理由で最大3タイル**（跳び越えが要るため）。

### ⬜シャレコのアートについて
スプライトは**手続き描画**（`sprites.js` の `buildSkully`/`buildSkullyBones`・22×20を2倍で表示）。
既存方針「画像アセットを増やさず手続きで描く」（洞窟タイル1.542／入場土管1.549／宝箱1.452）に合わせたもの。
⚠**骨に見えるかどうかは線の巧さではなく「骨と骨の間が透けているか」で決まる**（初版は胴を楕円で塗って
しまい毛のある小動物に見えた）。希望があれば [[piyo-sprite-motion-rule]] に沿った画像アセットへ差し替え可。

---

## 11.7 ✅P2-b 調整＋ボス闘技場の器（Ver.1.564〜1.565）

### ユーザー報告への対応（すべて実測で確認）
| 報告 | 原因 | 対応 |
|---|---|---|
| 地底でレベルアップしない | 1.541の地底ガードが**レベル計算より上**にあり早期returnしていた | レベル計算を分岐より前へ。12,300m/12,600mで上がるのを確認 |
| 上下移動で背景の柱が壊れる | 柱を固定長(44〜H-96)で描いて全体を縦にずらしていた＝端が画面内に入る | 胴を画面外まで通し、**柱頭リングと目地だけ**を流す。鍾乳石は額縁なので固定に戻した |
| 降りる時に下が見えず理不尽 | カメラが足元を常に画面 y=354 に置いていた | `descend` の部屋は足元を y=200 に。**下に見える範囲 96px→250px** |
| きぐるみ/サイバー/忍者の自動攻撃が出ない | **弾の消去判定が画面座標**（`b.y > GAME_HEIGHT+120`）＝地底では撃った次のフレームに消えていた | カメラ相対に変更。地上は camera.y=0 なので式の値は従来と完全に同じ |
| トゲの横判定がシビアすぎる | 既存の敵ダメージ判定 `aabbShrink(player,e,14,12)` は**両方を14px削る**（片側28pxの猶予）のに、トゲはプレイヤーを8px削るだけだった＝**game内で最も厳しい当たり** | プレイヤー14px＋トゲ6px削り＋**横に連なるトゲは1枚に結合**（1マスずつだと境目に安全な穴ができる）。実測で**1.563比13px甘く**なり「三角に14px食い込んでから当たる」 |
| 針の山が横に長い | 3タイル(96px)は踏切位置がタイル端でないと届かない | 全部2タイル(64px)へ。跳躍135pxに対し余裕71px |
| コウモリ追加 | — | 回廊2・熔炉2・王の廊下2 の計6体（`v`＝既存 bat_fly・図鑑は `enemy:flying_night` に相乗り） |
| アイテムを少し（回復2つ） | — | ハート2・レモン缶・シールド・エナジー・マグネットの計6個（マップ文字 `1`〜`5`） |
| 急降下斬りを地底で倍・左右均等 | — | `SAMURAI_DIVE_UG_MULT=2`。実測 **±14px/f で完全対称**（地上は右7/左10の非対称のまま） |

### ボス闘技場（ロックマン式・器のみ／本ボスはP3）
- レベル末尾に **部屋8「巫女の間」40タイル(1,280px)** を新設。最大画面幅1,150pxより広い＝**どの端末でも固定1画面**。
- 巫女の門を **89タイル**に詰め、その右端がちょうど `UG_TRAVEL_PX=16,000px`（=500タイル）。
  ⇒ **闘技場の左端＝カメラ終端**なので、闘技場に入った時点で800mの加算は完了しており、**ボス戦の長さは距離に一切影響しない**。
- 移行: 敷居をまたぐ → ①カメラが闘技場へ寄る＋数歩だけ自動歩行 → ②背後の扉が降りて地形に壁が足される → ③紫の渦からボスが実体化 → ④戦闘 → ⑤撃破演出→退場。
- ⚠**演出でcamera.xを直接動かす時は `ugDistOffset` も積むこと**（1.564で発覚）。積み忘れると
  その演出の間だけ距離が非圧縮で増え、実測で加算量が **800m→815m** にズレていた（バイオーム/ボス距離の補正も道連れで狂う）。
- ⚠中に居るのは**仮ボス**（HP100・踏み10ダメージ・シルエット描画・画面に "(placeholder)" と表示）。
  闘技場が成立するかの検証用で、P3で「闇の巫女」に差し替える。
- ⚠**ゴールデンエッグ（SPEC §8で毎回1個に決定）は本ボス実装まで保留**。仮ボスは10回踏むだけで倒せるため、
  今有効にするとエッグの希少性が壊れる。スコア10,000とコイン12枚は付与済み。

---

## 11.8 ✅長さ 1,200m 化＋敵の倍増（Ver.1.566・実プレイ後のユーザー判断）

**加算量は `UG_TRAVEL_PX × UG_DIST_SCALE ÷ 10` で決まる**ので、`UG_TRAVEL_PX` を 16,000 → **24,000px** にした（=1,200m）。
1,200m は `SPEED_UP_INTERVAL`(300m) のちょうど4レベル分＝**踏破中にきれいに4回レベルが上がる**（ユーザーの狙い）。

横幅は「カメラ終端 24,000px(=750タイル) の**次**が闘技場40タイル」になるよう、横の部屋だけを延ばして 750 タイルに揃えた。

| 部屋 | 1.565 | 1.566 |
|---|---|---|
| 落下の間 / 尖塔 / 深淵（縦） | 19 / 32 / 32 | 変更なし（縦は横の予算を使わない） |
| 石柱の回廊 | 94 | **145**（石橋＋溶岩の裂け目を2箇所に） |
| 熔炉の淵 | 109 | **175**（溶岩の池 6→10・火の玉 3→5） |
| 王の廊下 | 125 | **200**（天井ファイアバー 3→5・床のファイアバー 1→2・トゲ 5→9・逃げ場の足場 3→5） |
| 巫女の門 | 89 | **147**（門は最奥へ） |
| **合計（闘技場の手前まで）** | 500 | **750タイル = 24,000px** |

**敵は 20体 → 42体**（ニワトリ9・コウモリ14・シャレコ10・ひよこ7・アカバネ1）。コインも 29→41、アイテムは6のまま。

**✅実測（ボット通し・GAME_WIDTH=820）**: 全8部屋踏破 **160秒**（1.565は100秒）／
**加算ちょうど 1,200m**／**レベルアップちょうど4回**／闘技場に入った時点(phase2)で `dist=13,200` ＝加算完了・
**ボス戦中の加算 0m**／R7→R8／`stageShiftM = −1,200`／次のボス **15,600m**／
バイオーム境界 13,200・13,800・14,400・15,000（すべてキリの良い数）／退場後 `camera.y=0`・草原／コンソール0。

---

## 11.9 ✅怪しい老婆の店（Ver.1.567〜1.569）

### 置き場所と、それが品揃えを決めていること
店は **巫女の門（部屋7）＝全ギミックを抜けた後**にある。⚠だから**回避系（溶岩よけ・トゲ無効・炎無効）は
買った瞬間に用済み**になる（1.569でユーザー指摘により起案をやり直した）。ここに置けるのは次の3種だけ:
①ボス戦に効く ②地上へ戻った後の**残りのラン**に効く ③**永続**。

### 品揃え（✅ユーザー決定・実測済み）
| 品 | 価格 | 効果 | 実測 |
|---|---|---|---|
| 🍡 ごくらくまんじゅう | 9,000 | ライフ+3・**2個で売り切れ** | 4→7／2個目は10でクランプ／3個目は売り切れ／満タン時は購入ブロック |
| ⚗️ ろうばの げきやく | 8,000 | 30秒間エナジー弾（**ダメージ1**） | 1,800f＝30秒／2個で60秒に加算／2秒で6発（缶と同間隔）／全弾dmg1 |
| 👁 ちていの ぬしの かご | 200,000 | **永続**。以後、地底に入る時ライフ+2 | upgradesに保存／5→7・9→10でクランプ／購入後は陳列から消える |

**⚠劇薬の分岐位置が仕様そのもの**（`updateBullets`）: エナジー缶(dmg2)より**後**・スキンの自動攻撃(dmg1)より**前**。
缶を持っている間は強い方だけが出て二重に撃たず、金を払った劇薬はスキンより必ず優先される。

### 実装の勘所
- 品は **`STAGE_SHOP_ITEMS` に `ugOnly: true` で混ぜて置く**（tutorialOnly と同じ仕組み）。⚠別配列にすると
  `STAGE_SHOP_ITEMS.find(...)` が購入/売却/確認ボックスの**4箇所**にあるので全部直す必要が出る。
- 永続品は `permaUpgrade` を持たせ、`gameSettings.upgrades` に積んで `saveSettings()`。
  ⚠**買い切りは `stageShopLineup` で陳列から外して実現する**（`maxPerVisit` は訪問ごとの制限なので再訪で復活してしまう）。
- ⚠`applyUpgrades()` は**ラン開始時にしか走らない**ので、その場で効かせたい永続効果はここでは効かない。
  加護は「次に地底へ入る時」に効くので `enterUnderground()` 側で直接ライフを足している。
- ⚠**セリフは `setKeeperTextFor` の1箇所で差し替える**（`ug_` を前置したキーが辞書にあれば使う）。
  呼び出し側が10箇所以上あるので個別分岐だと必ず取りこぼす。用意していない文言は地上のものに自動で落ちる。
- ⚠`playStageBGM()` に地底分岐が要る。無いと**店を出た瞬間に地上のステージ曲が鳴る**（`confirmCloseShop` が呼ぶ）。
- ⚠`iconImg` は**存在しない画像を指すと壊れた画像アイコンが出る**（else が `item.icon` を使う実装）。
  アセットが無い品は `iconImg` を書かず絵文字だけにすること。

### ✅一枚絵2点（1.570・OpenAI生成）
生成は `tools/generate-ug-shop-scenes.mjs`。⚠**2点とも人物が写るので必ず edits（title.jpg 参照）**を使う
＝同一人物・同じ頭身を保つ唯一の方法（[[piyo-artwork-proportion-rule]]）。generations で作ると別人になる。
1. **`images/manju_scene.jpg`** — ごくらくまんじゅうを**おそるおそる食べる**シーン。
   ⚠**服はタイトル画面のメイド服のまま**・ツインテール維持（1.570ユーザー指定。他の一枚絵は黄色ワンピに
   着替えさせているが、この絵だけは例外）。⚠**「可愛い顔のまま緊張して汗をかいている」**が指定
   （1回目の生成は目つきが鋭く不機嫌そうになり却下された＝プロンプトに cute/round/big eyes と
   「NOT angry, NOT glaring, NOT half-lidded」を明示して解決）。
   ✅採用＝`_raw/ADOPTED_manju_scene.png`（作り直し2回目の2番）。
2. **`images/ug_shop01.jpg`** — 店内の絵。⚠**背景ではなく「ぴよ氏＋店員（老婆）が描き込まれた絵」**
   （1.570ユーザー指定）＝地上の `shop01`〜`05` と同じ方式。`#shopImgArea` は画面の右半分だけで、
   商品リストと老婆のセリフは左の別パネルに出るので、絵を暗く空ける必要はない。
   老婆は「不気味だが無害でどこか滑稽な魔女ばあさん」＝骸骨・流血にはしない（9+維持）。
   `setShopBg` の地底分岐でこの1枚に固定（地上のように shop01〜05 を切り替えない＝店は1軒しかない）。
   ⚠**ぴよ氏の表情**は「可愛い顔＋少し驚いた」で確定（1回目は「不機嫌そう」で却下＝プロンプトに
   big round sparkling eyes / slightly surprised / NOT grumpy を明示して解決）。
   ✅採用＝`_raw/ADOPTED_ug_shop01.png`（作り直し2回目の2番）。
   ⚠**画像の比率について**: `#shopImgArea` の縦横比は端末で変わる（実機の横長では約1.1、縦長の
   デバッグ画面では0.7）。`cover` で敷くので**縦長の画面では左右が大きく切れる**。実機の横長では
   老婆もぴよ氏も全身が収まることを実測済み。差し替える時は必ず横長比率で確認すること。

### ⬜（旧）未納品だった一枚絵2点の配線メモ
1. **`images/manju_scene.jpg`** — ごくらくまんじゅうを食べるシーン（そば/いちごショートと同じ全画面演出）。
   **✅作画指定（ユーザー・1.570）＝「おそるおそる食べる様子」**。老婆から買った怪しい饅頭を、
   ぴよ氏がこわごわ口へ運ぶ絵。⚠[[piyo-artwork-proportion-rule]] 厳守＝**デフォルメ絶対禁止・title.jpg参照のeditsで5-6頭身**。
   ⚠未納品でも壊れないよう `showSobaScene` に `img.onerror` を入れてある（画像だけ隠して暗幕＋回復量表示で成立）。
2. **店内の背景**（`shop01.jpg` 相当の洞窟の中の店）。今は暗い洞窟のグラデーションで代用中。
   用意できたら `setShopBg` の地底分岐を `url('images/ug_shop01.jpg')` に差し替えるだけ。

店員の顔アイコンは**その場で描いた32pxのドット絵**（`ugKeeperFaceURL()`）。正式な絵にするならこの関数を捨てて画像パスを指すだけ。

---

## 12. 実装フェーズ案

- **P0**: 本仕様の要確認①〜⑦をユーザーと確定。
- **P1**: サブステート基盤（save/restore・強制土管・落下導入・**追従カメラ＋左壁クランプ＋距離加算**）を**プレースホルダ地形**で通す（アート無しで往復し、距離が単調増加＝規定量ぶん増えることを先に実測する）。
- **P2**: レベルデータ＋地底タイル／背景／BGM。
- **P3**: 闇の巫女（ロジック→アート）。
- **P4**: 敵スケール・報酬・図鑑・アカバネ再調整・全体調整。

各フェーズで「地底の前後で距離が連続・Lv非飛躍・コンソール0」を実測する。

---

## 13. 要確認（2026-07-25 ユーザー回答済み）

1. ✅出現サイクルの剰余定義 → **毎回ゲート付き**（カカシ=6の倍数 / 地底=6の倍数+1）。カカシは1.3で稼働。
2. ✅強制土管の見た目 → **既存 item_pipe を拡大流用**（初版）。専用大穴は必要なら後日。
3. ✅追従カメラの縦方向 → **横主体＋軽い縦を採用**（ユーザー明示）。
4. ✅レイアウト種類数 → **初版は1種**（作り込み固定）。難度は敵のラウンドスケールで作る。
5. ✅地底ボスのモチーフ → **人型の巫女／魔女**（2026-07-25ユーザー決定）。詳細は「7. 地底ボス」。⚠ボス名に「魔女」は使わない（スキン「魔女ぴよ」と競合）。
6. ✅撃破報酬 → **ゴールデンエッグ1個（毎回）＋スコア10,000（倍額）＋コイン多め**。詳細は「8. 退場・報酬」。
8. ✅ボスの正式名称 → **闇の巫女**（やみのみこ・内部kind=`priestess`）。⚠「魔女」はスキン「魔女ぴよ」と競合するため不採用。
7. ✅地底での死亡 → **通常ゲームオーバー**（死なない部屋にはしない＝ボス戦だから）。

---

## 11.10 ✅P3「闇の巫女」本実装（Ver.1.570）

### アート＝**OpenAI API 生成のみ**（⚠ユーザー厳命）
手続き描画で作った初版は**却下**。「もっと人型であり、美しくも恐ろしい巫女の姿に」との指定により、
`tools/generate-boss-priestess-openai.mjs`（gpt-image-1）で3案生成 → **1番を採用** → `images/boss_priestess_idle.png`。
- **ちび頭身にしない**（[[piyo-artwork-proportion-rule]] と同方向＝デフォルメ禁止・6頭身の細身の人物）。
- 実寸 **104×132** を等倍で描く（他のボスは128×128だが、人型なので縦長のまま使う）。
  ⚠`UG_BOSS_W/H` はこの実寸と必ず一致させること。拡大縮小するとドットがボケる。
- ⚠**絵の中身は104pxのうち中央68px**で左右に18pxずつ透明の余白がある。これが当たり判定と噛み合っている:
  体当たりは `aabbShrink(player, b, 18, 14)` ＝両者を18px削るので、**ボスの箱がちょうど68px＝見えている体**になる。
  W を変えるならこの18も見直すこと。
- 目の位置(0.23h)と両手の玉の位置(0.42h)は**画像から実測**して `UG_BOSS_EYE_DY` / `UG_BOSS_ORB_DY` に入れた。
  光のにじみと呪弾の発射点はこの2つを見る＝絵と光が絶対にズレない。
- 🔜動きの差分コマが要るなら [[piyo-sprite-motion-rule]] に従い**Veo動画からコマ切り出し**（1本ずつユーザー確認）。

### 邪神の巨像（`images/ug_idol.png` 220×300・OpenAI生成3案から**3番**を採用）
- ⚠置き場所は**ボス部屋の中ではない**（ユーザー訂正）。**門の広間の一番奥＝ボス部屋に入る直前の「祭壇」**（巫女の門・列132）。
- マップ文字 **`I`**（当たり判定なしの飾り・1レベルに1体）。
- ⚠**像の正面（列126〜134）には石を一切置かない**（ユーザー指定「像の前にブロックは必要ない」）。
  まぐさを125で切って135から再開し、門柱128も抜いて**壁龕（アルコーブ）**にしてある。
- 描画は `drawUgIdol()`。⚠**decor（アーチの石）より先に描く**＝後に描くと像がアーチを塗りつぶす。

### ボスの中身（SPEC §7 の攻撃表をそのまま実装）
| フェーズ | 内容 |
|---|---|
| 共通 | 浮遊(上端916)＝**踏めない高さ**／詠唱で降下(上端1034)＝**踏める**。`exposed` が唯一の被踏条件 |
| P1 (HP>60%) | 呪弾（扇3発）／詠唱＝硬直＝踏みチャンス |
| P2 (60〜30%) | ＋瞬間移動（残光→渦→実体化）／魔法陣→光柱／呪弾5発 |
| P3 (30%〜) | ＋分身3体（**本物だけが撃つ**）／大詠唱（暗転＋安全地帯1箇所）／螺旋弾幕／呪弾7発 |
| 全フェーズ | **踏まれた直後は必ず反撃**（`counter`＝全方位の呪弾）＝カカシ1.535の教訓 |
| 60%/30%到達時 | `awaken`＝解放演出（暗転・光柱・輪・全方位弾）。この間は攻撃選択をしない |

- **HP200 / 踏み10 / 20回**（実測ちょうど20回で撃破）。弾・必殺も通る（装甲判定は置いていない＝
  浮遊中は床から撃った弾が届かない高さ関係そのものが「隙を突く」設計になっている）。
- 呪弾は **`bossState.eggs` を流用**（SPEC §7.2 の指定）。⚠そのため `updateUgBoss` から `updateEggs()` を呼ぶ必要がある。
- **報酬**: スコア10,000＋コイン12＋**ゴールデンエッグ1個**（`collectGoldenEgg` を通す＝図鑑登録と保存が集約されている）。
- **図鑑**: `boss:priestess`（撃破時のみ登録＝1.474の統一ルール）。ja/en の説明文つき。

### ボス戦中の乱入雑魚（ユーザー指定）
`ugSpawnArenaMobs()`。⚠通常ボスの `spawnEdgeEnemy` は `GROUND_Y` と `bossState.arena*` を見るので地底では使えない。
**地上の雑魚は右からだけ**（左は扉が閉じて壁になっている）／コウモリは左右から。同時数は `2 + bossEncounter()` 体まで。
⚠**大詠唱の開始時に雑魚を消す**（暗幕で見えない敵に当たるのは理不尽・演出としても「薙ぎ払われた」に見える）。

### ⚠実装中に見つけて直した既存バグ（どれも地底/ボスを作って初めて露見した）
1. **地底ボスのBGMが鳴っていなかった**。`audio.js` の `bossUndergroundBGM` が**コメントアウトのまま**で、
   呼び出しも `playBGM('boss_underground')`＝`this['boss_undergroundBGM']` を探して見つからず、
   `stopAllBGM` だけ効いて**無音**だった。正しい呼び名は `playBGM('bossUndergroundBGM' の type 部分)`＝`'bossUnderground'`。
2. **`updateEggs` の画面外除去が画面座標**（`egg.y > GAME_HEIGHT+50`）。縦カメラのある地底では
   闘技場のワールドy(860〜1180)が最初から条件を満たし、**撃った次のフレームに全部消える**
   （1.564でプレイヤーの弾に起きたのと同じ罠）。カメラ相対へ修正。地上は camera.y=0 なので式の値は不変。
3. **地底で足場に乗ると歩行が遅くなる**（ユーザー報告）。`player.groundType` を
   `getBiomeIndex(gameState.distance)` から決めていたため、`updateBiome` の「地底中は5に固定」が効かず、
   12,000m付近が砂漠(1)判定＝城の石積みが**流砂扱い**になっていた（歩行0.55倍・ジャンプ0.9倍）。地底は常に normal へ。
4. **地底でプレイヤーが地面から浮いて見える**（ユーザー報告）。`buildCaveTop` の砕けた上端が
   **y=1〜5から始まっていた**ため、当たり判定の面(y=0)との間に最大5pxの隙間ができていた
   （地上のタイルは草が足に重なるので同じ隙間でも目立たない）。上端を y=0 に届かせて解消。
5. **アカバネが攻撃する前に消える**（ユーザー報告）。予告(30フレーム)の間も共通処理
   `e.x += e.velX - gameState.gameSpeed` で流され続けていた。Lv17以降は gameSpeed=6.0 で頭打ちなので
   プレイヤーは画面左端クランプに押されて毎フレーム+6.0進み、鳥は-(|velX|+6.0)＝**距離が毎フレーム約14px縮む**。
   予告開始(240px)から30フレーム後には鳥は**195px後ろ**にいて、そこから真下へ急降下→離脱していた。
   → 予告中はスクロール分を打ち消し、上限速度(`DIVE_BIRD_LOCK_SPD`)でプレイヤーの真上へ寄せる。予告SEも追加。
6. **闘技場のボスが画面の外に出る**。闘技場は40タイル(1,280px)あるが**カメラは左端で止まる**ので
   実際に見えているのは GAME_WIDTH(820〜1,150)ぶんだけ。部屋の幅から比率で位置を決めていた仮ボスの
   置き方をそのまま使うと、狭い端末では渦もボスも画面外だった。位置は**画面から**出すよう全面的に直した。
7. **`ugMakeEnemy` のキー名**。`pe.kind` を見るのに `k:` で渡していて、エラーも出ないまま**一体も湧かない**。

### レベルの手直し（ユーザー報告）
- **熔炉の淵の溶岩10連続 → 7つに減らして間にギミックを挟んだ**（「バリエーションに飽きが来る」）。
  トゲ床(列46)／幅4の池を**動く床で渡る**(列78-81)／ファイアバー(列110・140、140はlen6の大薙ぎ)を追加。
- **老婆の店の手前が完全に無風だった → ギミックを2つ追加**。ファイアバー(列14)と溶岩の裂け目＋火の玉(列27-28)。
  ⚠**店の入口(列40〜44)には何も置かない**＝入店の上スワイプを狙う位置で被弾させない。

### 怪しい老婆の店の専用BGM
ユーザー提供 `Oddity Cabinet.mp3` を [[piyo-suno-audio-tags]] のとおり ffmpeg でタグとアルバムアート
（mjpegストリーム）を全除去し `sounds/shop_underground.mp3` として配置。`openStageShop` で
`undergroundState.active` なら `playBGM('shopUnderground')`。⚠地上のステージショップと同じ関数を通るので分岐が要る。

### ボス戦中の乱入雑魚の最終値（✅ユーザー決定1.570）
- **地上の雑魚のみ**（空中は入れない）。空中を混ぜると呪弾を避けるための縦の逃げ場まで塞がる。
- **同時 `1 + bossEncounter()` 体まで（1巡目=2体）／間隔およそ6〜8秒**（初版から約2倍に薄めた）。
- ⚠**ゼロにはしない**。闘技場はカメラが止まっていて距離が増えないので、ぴよフラッシュのゲージ加算源が
  **撃破(+4%/体)しか無い**（距離 0.08%/m はここでは効かない）＝雑魚を消すとボス戦中に必殺技を貯める手段が消える。
- 実測: 133秒の戦闘で**13体湧き・同時最大2体・飛行0体**・撃破11体で**ゲージ44%**（半分強）。連発はできない。

### 技のSE（✅ユーザー提供・1.570）
ユーザー提供の魔法SE4種をリネームして配線（元のファイル名と用途は無関係＝**音の質感で割り当てた**）。
[[piyo-suno-audio-tags]] と同じ手順でタグ・アルバムアートを ffmpeg で除去済み。
| 元ファイル | → | 用途 | 呼び名 |
|---|---|---|---|
| 雷魔法1.mp3 | `sounds/ug_boss_curse.mp3` | 呪弾（扇・螺旋・踏まれた直後の反撃） | `playUgCurse()` |
| 重力魔法1.mp3 | `sounds/ug_boss_sigil.mp3` | 魔法陣→光柱／瞬間移動／大詠唱の着弾 | `playUgSigil()` |
| 暗黒魔法.mp3 | `sounds/ug_boss_dark.mp3` | 大詠唱の開始（暗転） | `playUgDark()` |
| ステータス上昇魔法1.mp3 | `sounds/ug_boss_awaken.mp3` | フェーズ移行の解放 | `playUgAwaken()` |
⚠呪弾は連射するので音量を 0.45 と控えめにしてある（他は 0.6〜0.7）。

### ✅負荷の実測（1.570・ユーザー指定「重すぎないかチェック」）
**実rAFのフレーム間隔**で計測（＝プレイヤーが体感する値そのもの）。結果は**全ケースで 16.67ms＝60fps 維持**。
| シーン | 平均 | p95 | 判定 |
|---|---|---|---|
| 地上の通常ラン（基準） | 16.67ms / 60fps | 17.6ms | — |
| 地底ボス戦 P3（通常） | 16.67ms / 60fps | 17.6ms | ✅ |
| 螺旋弾幕（最大密度） | 16.67ms / 60fps | 17.6ms | ✅ |
| 大詠唱の暗転（全画面塗り） | 16.67ms / 60fps | 17.6ms | ✅ |
| 解放演出＋画面閃光 | 16.67ms / 60fps | 16.9ms | ✅ |
| **最悪同時**（幅1150＋弾40＋分身＋光柱4＋雑魚） | 16.67ms / 60fps | 16.8ms | ✅ |

⚠**計測方法の落とし穴**（次に測る人へ）: 最初は `gameLoop` を囲って `performance.now()` の差を取り、
GPUを同期させるために毎フレーム `getImageData(0,0,1,1)` を挟んだ。しかし**読み戻しそのものが約14ms**かかるため
「どのシーンも14ms」という無意味な結果になり、暗転だけが重いように見えた。**実rAFの間隔を測るのが正しい**。

⚠負荷試験で潜在バグを1つ発見して修正: 解放演出の輪は `((ap2*2.2 + i*0.33) % 1)` で半径を出しているが、
**JSの % は被除数が負だと負を返す**。timer を人為的に伸ばすと `ap2` が負になり `ellipse` の半径が負→
`IndexSizeError` で**そのフレームの描画が丸ごと落ちる**。通常プレイでは起きないが `ap2` を0..1にクランプした。

### ✅実測（Ver.1.570・ボット通し・GAME_WIDTH=820）
移行演出→戦闘→撃破→地上復帰まで通し。**踏みちょうど20回で撃破**／全モード（呪弾・詠唱・反撃・瞬間移動・
魔法陣・分身・螺旋・解放）が出現／**ボスが画面外に出た回数 0**／**闘技場での距離加算 0m（13,200mのまま）**／
距離が減った回数 0／同時の乱入雑魚 最大3体（上限どおり）／コンソールエラー 0。
大詠唱は安全地帯の外が実測 RGB(5,2,14)＝ほぼ黒、内側は(23,15,33)＝視認可。

---

## 13.5 ⚠1.570 の既知の未修正バグ（コミット直後の自動監査で確定・**次セッションで直す**）

コミット `622e557` の直後に6観点×12エージェントで監査し、**5件すべてが敵対的検証を生き残った**（反証0件）。
どれも1〜2行で直る。修正時は index.html を触るのでバージョンを **1.571** に上げること。

1. **[medium] リタイアで `undergroundState.pipeAnim` が残り、次ランのボーナス土管が地底へ繋がる**
   土管に沈む演出(39F)の途中でポーズ→リタイアすると、`pipeAnim=false` にする gameplay.js:1794 に到達しないまま
   resetGame が走る（index.html:4122 は pipePlaced/pipeX/pipeRise は戻すが pipeAnim を書かない）。
   次ランの通常土管で `updatePipeAnim` の完了判定が true になり `enterUnderground()` が走る。
   R1・300m地点で闇の巫女戦が始まり、`exitUnderground` の `bossDistanceFor(gameRound-1)` が `bossDistanceFor(0)` になって
   **以後ずっとバイオーム境界とボス距離が壊れる**＋エッグが土管ごとに稼げる。
   → index.html:4122 に `undergroundState.pipeAnim = false;` を1行。
2. **[medium] 詠唱の窓が閉じた瞬間、巫女が1ドットも動かないまま接触ダメージが確定**
   高度更新（gameplay.js:780-783）が switch より前・`low` は更新前の mode で評価されるので、`case 'cast'` が
   `b.exposed=false` にするフレームの巫女はまだ castY(1034)。同フレームの ugPriestessCollision が
   `aabbShrink(player,b,18,14)` で成立して被弾する。踏めば stompCd=40 で安全なので
   **「踏めた時だけ安全・踏み損ねると必ず殴られる」＝コメントが明記する設計意図と逆**。
   分身も同じ（`ugEndClones` の瞬間）。→ `case 'cast'` と `ugEndClones` 隣に
   `b.stompCd = Math.max(b.stompCd, UG_BOSS_RISE);`。⚠`ugEndClones` の**中**に置かない（踏み経路から呼ばれ無敵40Fを縮める）。
3. **[medium] 同じ問題の降下側** — castTele の後半 約16フレームは `exposed=false` のまま箱が重なる。
   → gameplay.js:1095 の接触判定に `var settled = (b.y <= b.hoverY + 80);` を足す。
4. **[medium] 地底のアカバネが攻撃前に消える（1.570の修正は地底では未解決）**
   `terrainTopAt`（index.html:6051）は「x を含む terrain のうち**最小の t.y**」を返すので、**天井のある地底では天井を返す**。
   部屋5「王の廊下」(topY=-548)の 'd' は floorY が -602 になり、降下1フレーム目で **264px上へ**ワープして天井を貫通・離脱。
   予告の照準線（render.js:2675）も同じ式なので**上向き**に伸びる。地上は天井が無いので同じ式で正しく動く。
   → `terrainTopBelow(x, fromY)`（fromY 以下で最小の t.y）を新設し、AIと予告描画の**両方**を差し替える
   （同じ関数にすることで照準線と着地点が必ず一致する）。床が無い時の fallback は地底なら `ugDeathY()`。
5. **[low] 地底ボス戦で広告復活するとボス曲が地底フィールド曲に差し替わる**
   闇の巫女は `undergroundState` 系で動くので `bossState.active` が false のまま＝adRevive が `playStageBGM()` に落ちる。
   → gameplay.js:23 の地底分岐を `playBGM(bossPhase === 4 ? 'bossUnderground' : 'underground')` に。

**副次指摘（未検証・同じ関数群なので一緒に見ると安い）**: 報酬コイン12枚を浮遊高度のワールドYに湧かせるとジャンプで届かない／
撃破時に闘技場の雑魚を消していないので勝利演出3秒の間に被弾しうる／大詠唱の暗転中に `bossState.eggs` を消していない
（雑魚は消している）／`ugPriestessDefeated` が `undergroundState.flash` を消さない。

**✅逆に「問題なし」と裏が取れた範囲**（再検査は二度手間）: 距離の不変条件／座標系の取り違え（#4以外）／
ボスAIのハング・報酬二重付与・カウンタ持ち越し／当たり判定と絵の一致／状態リセット（#1以外）／
BGM・SE・画像・i18n の配線と build-www の取り込み。詳細は自動メモリ [[piyo-next-session-ultracode]]。

---

## 14. 2巡目以降のボス強化（✅設計として記録・**実装は未着手**／2026-07-25 ユーザー指定）

> ユーザー指定:「ボスの二巡目以降は強くなっていく設計として記録しておいて。今は二巡目以降の調整に着手しなくていい。」

### 現状（=既に効いているもの）
| 対象 | HP | 行動 |
|---|---|---|
| 通常ボス5種＋闇のカカシ | `BOSS_MAX_HP + min(max(0, gameRound - BOSS_CYCLE_ROUNDS), BOSS_HP_ROUND_CAP) × BOSS_HP_PER_ROUND`<br>＝ R1-R6:100 ／ R8:120 ／ R13(カカシ2巡目):220 ／ R14以降:240で頭打ち | `bossEncounter()`＝`ceil(gameRound/7)` 連動 |
| **闇のカカシ**（✅ユーザー要望で確認済み） | R6=100 / R13=220 / R20=240(上限) | `updateBossAI_scarecrow` が enc で: 行動サイクル `encMul`(1→0.85→0.72→0.62)／腕薙ぎを満タンでも解禁(enc≥2)／薙ぎ確率(0.5→0.55→0.62)／召喚数(2→3→4→5、上限6)／enc≥3でカラスも召喚／expose窓の短縮(×0.92→×0.85) |
| **闇の巫女**（1.570） | **200で固定**（増分は未決） | `ugPriestessAI` が enc で行動サイクルを短縮（1→0.87→0.76→0.66）＋乱入雑魚の同時数 `2+enc` |

⇒ **カカシは2巡目以降ちゃんと強くなる**（HP・行動の両方）。巫女は行動だけが強くなり、HPは据え置き。

### 🔜今後決めること（着手はユーザー指示を待つ）
1. **闇の巫女のHPの伸び方**。通常ボスと同じ「+20/ラウンド・上限+140」に乗せるのか、
   ラスボス格として別カーブ（例: 遭遇ごとに+40・上限4段＝360）にするのか。
   ⚠決めたら `UG_BOSS_HP` を `bossEncounter()` 連動にするだけで入る（AIは既に enc を見ている）。
2. **P2/P3への移行しきい値**を周回で早めるか（60%/30% → 70%/40%）＝同じHPでも手数が増える。
3. **攻撃の追加解禁**（例: 2巡目から螺旋弾幕をP2でも使う／分身を4体にする）。
   ⚠数を増やすより**解禁を早める**方が、避け方の学習が無駄にならない（カカシで実証済みの考え方）。
4. **地底のギミック側**の周回強化（ファイアバーの角速度・火の玉の周期）。⚠現状は1周目の値で固定。
5. ⚠**避けにくさで難度を上げない**という原則は維持する（呪弾の角度差 `UG_BOSS_CURSE_STEP` や
   予告フレームは触らない＝「隙間を抜けて避ける」設計を壊さない）。強くするのは**手数と密度**で。
