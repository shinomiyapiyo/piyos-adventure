# RELEASE 1.7（2026-08-01）

⚠手順は `RELEASE_PLAYBOOK.md` が正。ここは**この版の実測値と提出の記録**。

| | 前回（1.6.1） | 今回（1.7） |
|---|---|---|
| 同梱ゲーム | Ver.1.696 | **Ver.1.715** |
| iOS | 1.6.1 / build 12（公開済み） | **1.7 / build 13** |
| Android | 1.6.1 / versionCode 11（クローズドテスト） | **1.7 / versionCode 12** |

## 版数の判定（⚠通常ルールの例外）

`CLAUDE.md` の固定ルールに機械的に当てると **1.6.2** だった（中身は絵の差し替え13件＋不具合修正2件＋
既存仕様の調整1件＋バランス変更1件で、**遊べるものは増えていない**）。

✅**ユーザー判断で 1.7 に決定**（2026-08-01）:「**グラフィックの大半を一新したので1.7で確定**」。
＝**一枚絵・カットインをほぼ全面的に作り直した版**という位置づけ。
⚠この例外を残す理由: ルールの字面（新ステージ／新キャラ／新モード／新画面）だけで判断すると 0.0.1 になるが、
**プレイヤーから見た変化の大きさ**で 0.1 を選ぶ判断がありうる、という前例。

## 中身（Ver.1.697〜1.715 の19コミット）

**一枚絵・カットインの作り直し 13件**（`ART_STYLE.md` を基準として整備しながら実施）
- `shop05`(退店) / `tutorial_clear` / `shortcake_scene` / `ug_shop01`(老婆の店) / `manju_scene` /
  `title_shop` / `shop01`・`shop02`・`shop04`(店内) / `soba_shop_scene` / `special_cutin`(ぴよフラッシュ) /
  `eyes_closeup`(目のアップ) / `skin_ninja_idle`(色相補正)
- 主に直した内容: 尻尾の除去／衣装を正へ／**年齢の統一**（幼く見える症状5点）／髪色の照り／
  スカートのひよこ3つ／ドット感の統一／ノイズ除去

**不具合修正 2件**
- 1.708 買えない時に必ず失敗の絵を出す（`shop04` が出ない経路があった）
- 1.712 ポーズ中に HUD とストック枠がポーズ画面より前面に出ていた

**既存仕様の調整 1件**
- 1.704 購入成功の絵を shop02/03 交互から**3パターンに集約**（`shop03.jpg` 廃止）

**バランス変更 1件**
- 1.715 **敵の難度の伸びを復活**（速度上限 7.0→8.2＝R29到達／アカバネ率を R11=30%→R38=60% の
  緩やかな上昇へ）。根拠は `SPEC.md` §13

**新しい仕組み 1件**（1.6.1 に間に合わなかった分）
- 1.697 土管部屋の「満杯時の交換ダイアログ」

## 成果物の実測

### Android（AAB）
```
android/app/build/outputs/bundle/release/app-release.aab
```
| 項目 | 実測 |
|---|---|
| サイズ | **61.29 MB**（前回 60.16 MB ＝ **+1.13 MB**） |
| 同梱ゲーム | Ver.1.715 |
| versionName / versionCode | **1.7 / 12**（マニフェストから文字列で確認） |
| 署名 | `META-INF/UPLOAD.RSA` あり＝署名済み |
| 混入 | **なし**（public 直下は js 8本＋images＋sounds のみ。tools/wall/node_modules/スクショ用/xcarchive なし） |
| `shop03.jpg` | **無い**（1.704で廃止したものが正しく消えている） |

⚠**+1.13MB の内訳＝差し替えた一枚絵**（images 全体で 6.2MB／sounds は 47MB で不変）。
主なもの: `special_cutin.png` 422KB / `shop01` 324KB / `shop04` 321KB / `shop02` 318KB /
`soba_shop_scene` 307KB / `title_shop` 286KB。旧版はいずれも 60〜190KB だった。

### iOS（xcarchive）
```
~/Library/Developer/Xcode/Archives/2026-08-01/PiyosAdventure-1.7-build13.xcarchive
```
| 項目 | 実測 |
|---|---|
| CFBundleShortVersionString | **1.7** |
| CFBundleVersion | **13** |
| 同梱ゲーム | Ver.1.715 |
| App.app | 60 MB |
| 混入 | **なし**（public 直下は Android と同一） |
| 署名 | TeamIdentifier 7LJ8QA6336（自動署名） |
| 結果 | **ARCHIVE SUCCEEDED** |

## 提出の記録

（提出時に追記）

## ⚠この版の未了事項

**実機テストプレイを一度もしていない**。1.697〜1.715 の全部が未確認。特に見るべきもの:
1. **1.715 のバランス変更** — しおりの R24 から再開して体感（アカバネが 10.4秒→7.0秒に1体）
2. **1.697 の交換ダイアログ** — 土管部屋で持ち物が満杯の時
3. **絵13枚** — 店の3パターン／たちぐいそば／いちごショート／ぴよフラッシュ（目のアップ含む）／忍者ぴよの立ち絵
4. **1.712 のポーズ** — HUD とストック枠が消え、再開で戻るか
