# ぴよ氏の冒険 — ゲーム仕様書

**バージョン:** Ver.1.271（基準仕様）
**最終更新:** 2026-06-11（§21 素材リストのみ 2026-07-08 / Ver.1.406 時点に更新）
**開発:** NullPo Works

> ⚠ 本書は Ver.1.271 時点の基準仕様です。以降の変更（ボス5体化・スキン特殊効果・まほうのポーチ・
> ボーナスステージ・タイトルショップのメニュー化 等）は **HANDOFF.md のバージョン別changelogが正**。

---

## 1. ゲーム概要

| 項目 | 内容 |
|------|------|
| ジャンル | 横スクロールアクション（エンドレスラン系） |
| プラットフォーム | Web（PWA）、iOS/Android予定（Capacitor） |
| 対応言語 | 日本語 / 英語 |
| 画面方向 | 横画面（ランドスケープ）固定 |
| 操作方法 | タッチ操作（モバイル） / キーボード操作（PC） |
| 解像度 | 820 × 450（内部キャンバス） |
| フレームレート | 60 FPS（固定タイムステップ） |
| レンダリング | Canvas 2D（ピクセルアート調） |
| データ保存 | localStorage |

### コンセプト
プレイヤーキャラクター「ぴよ氏」を操作し、自動スクロールするステージを走り抜ける。敵を踏んで倒し、コインを集め、パワーアップアイテムを活用しながら、2400mごとに出現するボスを撃破してラウンドを進める。

---

## 2. 画面構成

### 2.1 スプラッシュ画面
- 注意事項の表示（「本作品にはゲーム内での軽い接触表現が含まれます」）
- クレジット表示（Music by NullPo Works / Created by NullPo Works）
- タップで次画面へ遷移

### 2.2 タイトル画面
- ゲームタイトル「ぴよ氏の冒険」表示
- タイトル背景画像
- BGM: `title.mp3`
- ログインボーナスポップアップ（該当時）

**ボタン一覧:**
| ボタン | 機能 |
|--------|------|
| 画面タップ / START | ゲーム開始 |
| RANKING | ランキング画面を開く |
| SHOP | タイトルショップを開く（現在グレーアウト） |
| SETTINGS | 設定画面を開く |
| GUIDE | 遊び方ガイドを開く |

### 2.3 ゲーム画面（プレイ中）
- HUD（左上）: HP（ハートアイコン）、スコア、距離、パワーアップ残り時間
- ポーズボタン（右上）
- コントロールバー（画面下部）: 操作ガイド表示
- タッチ操作エリア（透明）

### 2.4 ショップ画面（ステージ中）
- ボス出現250m手前の安全地帯でショップ建物が出現
- 建物付近で「↑スワイプで入店」プロンプトが表示される
- 建物付近にいれば**画面のどこで上スワイプしても**入店できる
- ドラクエ風メニューUI
- メニュー: 買う / 売る / 預ける / 広告視聴 / 出る
- 店主の背景画像が状態に応じて切り替わる

### 2.5 ボス戦画面
- WARNING演出（2秒間の警告表示）
- ボス登場アニメーション
- ボスHP表示バー
- 戦闘アリーナ（画面スクロール停止）

### 2.6 ゲームオーバー画面
- リザルト表示: 距離、スコア、撃破数、スピードレベル
- 広告リバイブボタン（条件付き表示）
- BGM: `gameover.mp3`

**ボタン:**
| ボタン | 機能 |
|--------|------|
| リトライ | ゲームを最初からやり直す |
| タイトルへ | タイトル画面に戻る |

### 2.7 ハイスコア名前入力画面
- ハイスコア達成時に表示（⚠**なまえを登録している場合は表示しない**＝2.9 の「なまえ」参照）
- 名前入力（最大10文字）
- NGワードフィルター搭載（部分一致。カタカナ→ひらがな・全角→半角等を正規化してから判定）
- ホワイトリスト機能あり（`NG_WHITELIST`）: 「かすみ」等の正当な名前がNGワード（カス等）に誤判定されるのを防ぐ
- ランキングサーバー（Firebase）へ送信
- 未登録の場合のみ「スキップ」を選べる（名無しのぴよ氏として記録）

### 2.8 ランキング画面
- Firebase Realtime Databaseからデータ取得
- BGM: `ranking.mp3`

**ランキングカテゴリ:**
| カテゴリ | 単位 |
|----------|------|
| 金額（スコア） | 円 |
| 距離 | m |
| 撃破数 | 体 |
| レベル | レベル |
| 連続コンボ | コンボ（1.745〜） |

- 各カテゴリ上位20位まで表示
- 同点の並びは 距離 → 金額 → 先着 の順で決める（コンボは同点が起きやすいため）
- ⚠コンボは 1.657 から記録しているので、それ以前の記録は値を持たない＝一覧から除外する

### 2.9 設定画面
| 設定項目 | 内容 |
|----------|------|
| サウンド | ON / OFF 切替 |
| 言語 | 日本語 / English 切替 |
| 遊び方 | 遊び方ガイド画面へ |
| コントローラー | コントローラーの操作説明（絵つきの専用ページ・1.743〜） |
| **なまえ** | ランキング用の名前を登録（任意）。下記 |
| データ引き継ぎ | 発行 / 入力 |
| セーブデータを削除 | 二段確認つき |
| プライバシーポリシー | 外部リンク |
| 利用規約 | 外部リンク |

**なまえの登録（1.747〜・ユーザー決定）**
- 登録は**任意**。登録しておくとハイスコア時に**名前入力の画面を出さずそのまま記録**する。
- ⚠**登録している時は「スキップ」を出さない**（2026-08-25 ユーザー決定）。
  手間を省くための機能なので、確認や見送りの口は設けない。**後から足さないこと。**
- **変更・削除はいつでもできる**（設定 → なまえ → へんこう／けす）。
- ⚠**名前のフィルタは送信時とまったく同じものを使う**（`sanitizePlayerName` / `playerNameError`）。
  登録・送信・保存の読み込みの3か所すべてが同じ規則を通る＝登録経由で抜け道を作らない。
  読み込み時にも通すので、NGワードが後から増えても古い登録名はすり抜けない。

### 2.10 遊び方ガイド画面
- セクション: 操作方法、アイテム、ボス、お金、ショップ、貯金、タイトルショップ、データセーブ、ヒント

### 2.11 ポーズ画面
- 「ポーズ中」表示
- 再開ボタン / リタイアボタン
- 画面タップでも再開可能
- 隠し機能: ポーズタイトルを3秒以内に10回タップ → デバッグモード切替

---

## 3. 操作方法

### 3.1 タッチ操作（モバイル）

| エリア | 位置 | 操作 |
|--------|------|------|
| 左移動 | 画面左端 0-18% | タッチ中に左へ移動 |
| 右移動 | 画面左端 18-33% | タッチ中に右へ移動 |
| デッドゾーン | 画面中央 33-70% | 操作なし（誤タップ防止） |
| ジャンプ | 画面右端 70-100% | タッチでジャンプ |

**ジェスチャー:**
| ジェスチャー | 動作 |
|--------------|------|
| 下スワイプ（15px以上 / 500ms以内） | 足場をすり抜ける |
| 上スワイプ（-20px / 500ms以内） | ショップに入る（建物付近にいれば画面のどこでも検出） |

### 3.2 キーボード操作（PC）

| キー | 動作 |
|------|------|
| ← / A | 左移動 |
| → / D | 右移動 |
| Space / ↑ / W | ジャンプ |
| ↓ / S | 足場すり抜け |
| Escape / P | ポーズ |

---

## 4. プレイヤー

| パラメータ | 値 |
|-----------|-----|
| サイズ | 48 × 48 px |
| 初期HP | 5（最大10） |
| 移動速度 | 6 px/frame |
| ジャンプ力 | -16 (上方向速度) |
| 重力 | 0.7 px/frame² |
| 最大落下速度 | 15 px/frame |
| 無敵時間（被弾後） | 180フレーム（3秒） |
| 地面Y座標 | 348 px |

### スプライトアニメーション
- `player_idle`: 待機
- `player_walk`: 歩行
- `player_jump`: ジャンプ上昇中
- `player_fall`: 落下中

### 地形タイプによる移動補正

| 地形 | 加速倍率 | 最大速度倍率 | 摩擦 | ジャンプ補正 |
|------|---------|------------|------|------------|
| 通常 | 1.0× | 1.0× | 0.85 | 1.0× |
| 流砂（砂漠） | 0.6× | 0.55× | 0.70 | 0.9× |
| 氷（雪山） | 0.4× | 1.0× | 0.97 | 1.0× |

---

## 5. 敵キャラクター

### 5.1 地上の敵

| タイプ | サイズ | 出現確率 | 移動速度 | 特徴 |
|--------|-------|---------|---------|------|
| ひよこ（chick） | 42×38 | 50% | 1 ± ランダム×1 | 基本的な敵 |
| 金ひよこ（golden_chick） | 46×42 | 25% | 1 ± ランダム×1.5 | やや速い |
| 母ひよこ（mama_chick） | 50×46 | 25% | 0.5 ± ランダム | 大きいがゆっくり |

- 出現間隔: 90フレームごとに40%の確率
- ラウンドごとに速度が `(1 + (ラウンド - 1) × 0.3)` 倍に増加

### 5.2 飛行敵

| パラメータ | 値 |
|-----------|-----|
| サイズ | 60 × 54 px |
| 水平速度 | -(2 + ランダム×2) × ラウンド補正 |
| 飛行パターン | サイン波（振幅0.5） |
| 出現高度 | Y: 82 〜 246 px |
| 出現間隔 | 180フレームごとに15%の確率 |

### 5.3 ボス戦ミニオン

**地上ミニオン（ラウンド2+）:**

| パラメータ | 値 |
|-----------|-----|
| サイズ | 42 × 42 px |
| 速度 | 1.2 + ランダム×0.8（1.2〜2.0） |
| 初回出現 | 180フレーム後 |
| 再出現間隔 | max(90, 180 - (gameRound - 2) × 20) フレーム |

**飛行ミニオン（ラウンド3+）:**

| パラメータ | 値 |
|-----------|-----|
| サイズ | 56 × 50 px |
| 速度 | 1.0 + ランダム×0.5（1.0〜1.5） |
| 出現高度 | Y: 80〜230 px |
| 初回出現 | 240フレーム後 |
| 再出現間隔 | max(120, 240 - (gameRound - 3) × 20) フレーム |

### 倒し方
- 敵の上から踏みつける（プレイヤーが上から着地する判定）
- エナジー弾で撃破
- シールド発動中に接触で撃破

---

## 6. ボスバトル

### 6.1 出現条件
- 2400mごとに出現（ラウンド1: 2400m、ラウンド2: 4800m、ラウンド3: 7200m…）
- 出現250m前に安全地帯（ショップあり）

### 6.2 ボスステータス

| パラメータ | 値 |
|-----------|-----|
| サイズ | 128 × 128 px |
| 基本HP | 10 |
| HPスケーリング | 10 + max(0, ラウンド - 3) × 3 |
| 撃破スコア | 5,000点 |
| ドロップコイン | 25枚 |
| 警告演出 | 2秒（120フレーム） |
| 怒りモード持続 | 2.5秒（150フレーム） |

### 6.3 戦闘フェーズ

**5段階のフェーズ:**
| フェーズ | 名称 | 内容 |
|---------|------|------|
| 1 | WARNING | 警告演出（2秒） |
| 2 | ENTERING | ボス登場演出（右から歩いて入場、速度2px/f） |
| 3 | COMBAT | メインバトル |
| 4 | DEFEATED | 撃破演出（5秒、爆発15Fごと×5回、90F目にコイン散布） |
| 5 | TRANSITION | ラウンド移行（3秒表示後にgameRound++） |

### 6.4 ボスAI（HP残量による行動変化）

**関数構造:** `updateBossAI(b)` → ディスパッチ関数 → `updateBossAI_mama(b)` を呼び出し

| AIフェーズ | HP残量 | 速度倍率 | 召喚間隔 |
|-----------|--------|---------|---------|
| フェーズ1 | 70-100% | 1.0× | 300F (5秒) |
| フェーズ2 | 30-70% | 1.5× | 240F (4秒) |
| フェーズ3 | 0-30% | 2.0× | 180F (3秒) |

**フェーズ1の攻撃パターン:**

| 攻撃 | 確率 | 条件 | 攻撃後の待機 |
|------|------|------|------------|
| 閃光アタック | 15% | ラウンド2以降のみ | チャージ50F→閃光30F |
| 巡回のみ | 85% (R1) / 残り | — | 180F |

**フェーズ2の攻撃パターン:**

| 攻撃 | 確率 | 詳細 | 攻撃後の待機 |
|------|------|------|------------|
| 閃光アタック | 15% | チャージ45F→閃光30F | — |
| 突進 | 20% | 速度5px/f × speedMult | 120F |
| 卵攻撃 | 20% | 3発、ベース速度4px/f | 120F |
| 火炎放射 | 25% | 60F持続、10Fごとに弾発射（計6発） | 120F |
| 巡回継続 | 20% | — | 90F |

**フェーズ3の攻撃パターン:**

| 攻撃 | 確率 | 詳細 | 攻撃後の待機 |
|------|------|------|------------|
| 閃光アタック | 15% | チャージ35F→閃光30F | — |
| 突進 | 15% | 速度5px/f × speedMult | 60F |
| 卵攻撃 | 20% | 4発、ベース速度4px/f | 90F |
| ジャンプ踏み | 15% | velY=-14 | 90F |
| 火炎放射 | 20% | 90F持続、10Fごとに弾発射（計9発） | 80F |
| 巡回継続 | 15% | — | 60F |

### 6.5 ボス攻撃パターン詳細

| 攻撃 | パラメータ |
|------|----------|
| 突進 | 速度5px/f × speedMult、プレイヤーに向かって水平移動、距離15px未満で停止 |
| 卵攻撃 | 拡散角度±0.3rad、弾速4px/f、重力+0.15px/f²、最大寿命300F |
| 火炎放射 | 弾速(5+乱数×2)px/f、弾サイズ24×24px、10Fごと発射 |
| ジャンプ踏み | velY=-14、重力で落下、地面着地で終了 |
| 閃光アタック | チャージ後に閃光30F、地面近く(GROUND_Y-70以内)のプレイヤーにダメージ |

### 6.6 ボスへのダメージ方法

| 方法 | ダメージ | 備考 |
|------|---------|------|
| 踏みつけ（ストンプ） | 1 HP | プレイヤーがボスの上30%に着地、+500点 |
| エナジー弾 | 0.2 HP/発 | 5発で1HP相当 |

### 6.7 怒りモード
- ストンプ命中後に発動（2.5秒間 = 150F）
- 移動速度が2倍に
- 連続ストンプ不可（クールダウン90フレーム）
- 突進・ジャンプ等の実行中攻撃をキャンセル

---

## 7. ステージ構成

### 7.1 自動スクロール

| パラメータ | 値 |
|-----------|-----|
| 基本スクロール速度 | 1.2 px/frame |
| 速度上昇間隔 | 300mごとに+20% |
| 最大速度 | 基本の5倍（6.0 px/frame） |
| 安全地帯速度制限 | 1.5 px/frame固定上限 |
| ボス戦中 | スクロール停止 |

### 7.2 バイオームシステム

2400mの周期で4つのバイオームが循環（各600m）:

| インデックス | バイオーム | 地形タイプ | 特殊効果 |
|------------|-----------|-----------|---------|
| 0 | 草原 | 通常 | なし（穴あり） |
| 1 | 砂漠 | 流砂 | 移動速度低下、穴なし |
| 2 | 雪山 | 氷 | 慣性増加（滑る） |
| 3 | 夜 | 暗い地形 | rgba(5,5,30,0.35)の夜間オーバーレイ、星空演出 |

- バイオーム間はグラデーションで滑らかに遷移（約2秒間）

### 7.3 地形

| 地形タイプ | 説明 |
|-----------|------|
| ground | 通常の地面 |
| hole | 穴（落下でダメージ） |
| elevated | 高台（60-100px） |
| ice_elevated | 氷の高台 |
| quicksand_elevated | 流砂の高台 |

- 穴出現確率: 15%（砂漠では穴なし、高速時は `max(0, 0.15 × (5.0 - speedMult))` に減少）
- 安全地帯（ボス手前250m）: 平坦地形のみ

### 7.4 足場（プラットフォーム）

出現間隔: 120フレームごとに60%の確率

**基本タイプ:**
| タイプ | 出現確率 |
|--------|---------|
| 雲 (cloud) | 67% |
| 浮遊地面 (floating_ground) | 33% |

**特殊足場（20%の確率で付与）:**

| タイプ | 確率 | 効果 |
|--------|------|------|
| 動く足場 (moving) | 8% | 上下に振動（振幅40-70px） |
| 消える足場 (disappearing) | 6% | 乗ると1.5秒後に消滅 |
| バネ足場 (spring) | 6% | 乗ると通常の1.4倍の高さでジャンプ |

- サイズ: 幅120-200px × 高さ30px
- 出現高度: Y 164-286px

---

## 8. アイテム・パワーアップ

### 8.1 フィールドドロップ

出現間隔: 480フレームごとに6%の確率

| アイテム | 出現確率 | 持続時間 | 効果 |
|---------|---------|---------|------|
| レモン缶 | 50% | 5秒 (300F) | ジャンプ力1.3倍 |
| エナジー | 14% | 8秒 (480F) | 自動で弾を発射（20F間隔） |
| シールド | 13% | 5秒 (300F) | 全ダメージ無効＋接触で敵撃破 |
| マグネット | 12% | 10秒 (600F) | 200px範囲内のコイン自動回収 |
| ハート | 11% | 即時 | HP+1（最大10）、満タン時は+1000点 |

### 8.2 ショップ版パワーアップ（持続時間比較）

| アイテム | ショップ版持続時間 | フィールド版との倍率 |
|---------|-----------------|-------------------|
| レモンスペシャル | 20秒 (1200F) | 4倍 |
| バリア | 10秒 (600F) | 2倍 |
| フルチャージ（全4種） | 15秒 (900F) | — |

### 8.3 コイン

| パラメータ | 値 |
|-----------|-----|
| 基本スコア | 150点 × コインボーナス倍率 |
| マグネット引き寄せ範囲 | 200px（アップグレードで拡張可） |
| 出現間隔 | 60フレームごと |

### 8.4 ボスアリーナドロップ（ラウンド3未満のみ）

| アイテム | 出現確率 |
|---------|---------|
| エナジー | 50% |
| ハート | 30% |
| シールド | 20% |

- 初回出現: 480F（8秒）後
- 再出現間隔: 9-15秒（540+ランダム×360F）
- 寿命: 10秒（600F）で消滅
- **ラウンド3以降はアイテムドロップなし**（既存アイテムの寿命管理のみ）

---

## 9. ショップシステム

### 9.1 ステージショップ（ゲーム中）

ボス出現250m手前の安全地帯に出現。ゲーム中のスコアで購入。

| アイテム | 価格 | 購入上限/回 | 効果 | ストック |
|---------|------|-----------|------|---------|
| 回復薬 | 6,000 | 2個 | HP +2（即時、上限10） | × |
| 回復薬（持ち帰り） | 12,000 | 1個 | HP +2（使用時、上限10） | ○ |
| バリア | 3,000 | 2個 | シールド 10秒 (600F) | ○ |
| レモンスペシャル | 1,200 | 2個 | レモン効果 20秒 (1200F) | ○ |
| フルチャージ | 5,000 | 1個 | 全パワーアップ 15秒 (900F) | ○ |
| リバイブポーション | 25,000 | 1個 | 手動使用: HP+2＋シールド5秒(300F) / 自動復活: HP=2＋無敵3秒(180F) | ○ |

**ショップ機能:**
- **買う:** アイテム購入
- **売る:** ストックアイテムを購入価格の50%で売却
- **預ける:** 現在スコアの50%を貯金に預入（1回/来店）
- **広告視聴:** スコアの30%を獲得（最低100、最大3,000）

### 9.2 タイトルショップ（永続アップグレード）

貯金を使って永続的な強化を購入。

| アップグレード | 最大Lv | 価格 | 効果 | 備考 |
|--------------|-------|------|------|------|
| コインマスター | 3 | 5k / 15k / 30k | コイン獲得量 +10/20/30% | プレミアム（準備中） |
| タフネス | 3 | 200k / 500k / 1M | 初期HP +1/2/3（5→6/7/8） | |
| ストック拡張 | 2 | 200k / 500k | 持てるアイテム 3→4→5枠 | |
| マグネットブースト | 2 | 50k / 125k | マグネット範囲 200→300→400px | |
| コンボマスター | 1 | 50k | コンボ猶予時間 1秒→1.5秒 | |
| 俊足 | 1 | 50k | 移動速度 1.3倍 | セール（元100k） |
| リバイバルフェザー | 2 | 500k / 1M | 復活 1/2回 per ラン | |
| **みきりの目** | 3 | 500k / 1M / 2M | **3秒間だけ世界が1/4速になる**。レベル＝1ランで使える回数 | 1.748〜 |

**みきりの目（1.748〜）**
- 高額レーンで初めての**「強くならない」品**。ここぞの一手を買う枠。
- **レベルで増えるのは回数だけ**（効果時間3秒・1/4速は据え置き）。強さが伸びると結局これまでと
  同じ永続強化になってしまうため（ユーザー決定）。
- 発動: 画面上部中央のボタン（必殺技ゲージの真下）／コントローラーは **L2 または R2**。
- ⚠実装は**gameLoop の固定ステップを間引く方式**。敵・弾・プレイヤー・演出がまとめて同じ割合で
  遅くなる＝要素ごとに速度を掛ける方式のような「掛け忘れた物だけ速いまま」が起きない。
- ⚠**残り時間は実時間で数える**（間引く前に減らす）。間引いた側で数えると3秒が12秒になる。
- ⚠**ランキングは汚れない**。距離は camera.x 由来なので、使うと実時間あたりの前進も1/4になる
  ＝稼ぎに使えない（むしろ距離は損をする＝「切り抜けるための道具」として筋が通る）。
- ⚠残り回数は**しおり（中断セーブ）に保存する**。しないと中断のたびに満タンに戻る抜け道ができる。
- ⚠ポーズ中はHUDを隠す（`body.is-paused #mikiriUI`）。入れ忘れるとポーズ幕の上に残る。

---

## 10. スコア・コンボシステム

### 10.1 スコア獲得

| アクション | スコア |
|-----------|-------|
| コイン取得 | 150 × コインボーナス倍率 |
| 地上敵撃破 | 200 × コンボ倍率 |
| 飛行敵撃破 | 300 × コンボ倍率 |
| パワーアップ取得 | 100 |
| HP満タン時ハート取得 | 1,000 |
| ボス踏みつけ | 500 |
| ボス撃破 | 5,000 |

### 10.2 コンボシステム

| コンボ数 | 倍率 | 演出 |
|---------|------|------|
| 1 | 1.0× | なし |
| 2 | 1.5× | コンボテキスト表示 |
| 3 | 2.0× | コンボテキスト表示 |
| 4 | 2.5× | コンボテキスト表示 |
| 5+ | 3.0× | 画面シェイク＋フラッシュ |

- コンボ猶予: 60フレーム（1秒）
- 5の倍数到達時にスクリーンフラッシュ
- コンボマスターアップグレードで猶予1.5秒（90F）

---

## 11. 経済システム

### 11.1 通貨

| 種類 | 用途 | 獲得方法 |
|------|------|---------|
| スコア（円） | ステージショップでの買い物 | ゲーム中のコイン・敵撃破 |
| 貯金 | タイトルショップのアップグレード購入 | ショップで預入、ログインボーナス |

### 11.2 ログインボーナス

7日周期で毎日報酬を獲得（連続ログインでカウントアップ）:

| Day | 報酬 |
|-----|------|
| 1 | 3,000 貯金 |
| 2 | 5,000 貯金 |
| 3 | 回復薬ストック |
| 4 | 7,000 貯金 |
| 5 | 回復薬ストック |
| 6 | 10,000 貯金 |
| 7 | 13,000 貯金 |

**不正対策:**
- 時計巻き戻し検知
- 7日間で受取上限7回
- 受取履歴タイムスタンプ記録

**ログボ強化パス（課金）:** 報酬2倍（30日間有効）

---

## 12. ストック（インベントリ）システム

| パラメータ | 値 |
|-----------|-----|
| 初期スロット数 | 3 |
| 最大スロット数 | 5（アップグレードで拡張） |
| 使用方法 | ゲーム中に手動使用 |

- ストック対象: バリア、レモンスペシャル、フルチャージ、回復薬（持ち帰り）、リバイブポーション
- ログインボーナスのアイテム報酬は `pendingStockItems` に保存され、次回ゲーム開始時にストックへ追加

### 12.1 満杯時の交換ダイアログ（**Ver.1.697 で実装済み**／仕様は 2026-07-31 ユーザー決定）

**目的**: 土管ボーナス部屋で持ち物が満杯のとき、いま持っている物と**引き換えて**受け取れるようにする。
現状（1.694〜）は満杯の品に触れると**自動で貯金へ換金**（売値＝定価の半分）されるだけで、選択肢が無い。

⚠**前提として動かさない決定**（既出）:
- **部屋の中身は所持状況で変えない**（満杯でも3個並べる。「空きゼロならコインに振替」は1.694で撤去済み）
- **部屋の中でアイテムを"使える"ようにはしない**。部屋では本編の更新が止まる＝バフのタイマーが減らず
  「部屋で全部かけてから出る」が最適手になるため（必殺技を部屋で禁止しているのと同じ理由）

#### 出る条件
- **土管ボーナス部屋の中だけ**。¥バッジの付いた品（＝`stockHasRoom` が false の品）に触れた瞬間。
  部屋は敵なし・死亡なし・世界が止まっているのでダイアログを出しても事故にならない。
- **宝箱（ラッキーの間）の報酬も対象**。フタが開く演出の直後に同じダイアログを出す。
- ⚠**本編（ステージ上のドロップ品・ステージショップ）では出さない**。本編は時間が進むので、
  ダイアログを読んでいる間に死ぬ。従来どおり自動換金のまま。
- チュートリアル中は対象外（今も換金せず、その場に残る）。

#### UI
- **画面中央の小さなパネル**（全画面モーダルにはしない＝部屋のテンポを切らないため）。
  「〈新しい品〉を もらう？」＋**手持ちアイコンを横一列**（最大6〜7個）＋「そのまま お金にする」。
- 手持ちアイコンをタップ＝それを手放して受け取る。**右のストック枠は選択対象にしない**
  （誤タップでアイテムを使ってしまう事故を避ける）。ダイアログ中はプレイヤーの入力を止める。
- **「この部屋は ぶん お金にする」**を置く。押すとその部屋にいる間は二度と聞かない
  （ポーションの間＝3個連続で聞かれるのを避ける）。部屋を出ると解除。

#### 手放した品の扱い（⚠悪用防止の肝）
**枠の種類ではなく「その品の出自」で分ける。**

| 手放した中身 | 判定 | 結果 |
|---|---|---|
| **ポーチが所有している品そのもの** | `!temp && base === id` | **換金しない**。今ランだけ不在になり、**翌ランで戻る** |
| それ以外（今ラン拾った／買った品） | 上記以外 | **売値（定価の半分）で貯金へ**＝既存 `convertItemToSavings` と同じ |

⚠ポーチの所有品を換金にすると「毎ラン無料で補充される品を毎ラン売る」＝**無限にお金が増える**。
ポーチ転売対策（1.526）と同じ穴なので、ここは絶対に塞ぐこと。

#### 入る場所
- **手放した枠にそのまま入れる**。ポーチの枠なら `base` は触らず `temp = true`
  ＝既存の「今回かぎりの補充」（`addToStock` の route 2）と同じ経路に乗せる。翌ランのポーチは元通り。
- ふっかつやく（`PERMA_STOCK_EXCLUDE`）もこの経路なら入れてよい（1.683 で確立済み）。
- 受け取った品は `markZukanSeen('item:<id>')` する（拾った時と同じ）。

#### 実装の要点（1.697）
| 場所 | 役割 |
|---|---|
| `index.html showStockSwapDialog()` | パネルのDOM。resolve = `{action:'swap',index}` / `{action:'sell'}` / `{action:'sellAll'}` |
| `gameplay.js takeRoomStockItem()` | 部屋での入手の入口（床の品も宝箱もここを通る）。戻り値 true=消費した |
| `gameplay.js isPermaOwnedContent()` | **`base === id` で判定**。temp フラグではなく実体で見る＝フラグが壊れても換金側へ倒れない |
| `gameplay.js applyStockSwap()` | 枠の入れ替え。金枠は `base` を触らず `temp=true` |
| `updatePipeRoom()` 冒頭 | ダイアログ中は部屋ごと停止（歩いて別の品に触れる／出口ゲージが溜まるのを防ぐ） |
| `pipeRoomState.autoSell` | 「ぶん おかねに」。`initPipeRoom` で毎入室 false |
| `BACK_HANDLERS`（core-state.js） | Android戻る＝「そのまま おかねにする」。**部屋のエントリより前**に置く |

---

## 13. サウンドシステム

### 13.1 BGM（MP3ファイル）

| ファイル | 使用場面 | 音量 | ループ |
|---------|---------|------|--------|
| title.mp3 | タイトル画面 | 0.6 | ○ |
| stage.mp3 | ステージ（ラウンド1,4,7…） | 0.5 | ○ |
| stage2.mp3 | ステージ（ラウンド2,5,8…） | 0.5 | ○ |
| stage3.mp3 | ステージ（ラウンド3,6,9…） | 0.5 | ○ |
| boss.mp3 | ボス戦 | 0.6 | ○ |
| shop.mp3 | ショップ | 0.5 | ○ |
| gameover.mp3 | ゲームオーバー | 0.7 | ○ |
| ranking.mp3 | ランキング画面 | 0.6 | ○ |
| win.mp3 | ボス撃破ファンファーレ | 0.7 | × |

### 13.2 SE（効果音）

**MP3ファイル:**
| ファイル | 使用場面 | 音量 |
|---------|---------|------|
| select.mp3 | 選択音 | 0.5 |
| or.mp3 | カーソル移動音 | 0.5 |
| flash.mp3 | フラッシュ攻撃音 | 0.5 |
| warning.mp3 | ボス出現警告音 | 0.5 |

**WebAudio合成SE:**
| 効果音 | トリガー |
|--------|---------|
| playJump() | ジャンプ |
| playKill() | 敵撃破 |
| playItem() | パワーアップ取得 |
| playCoin() | コイン取得 |
| playDamage() | 被弾 |

---

## 14. ビジュアルエフェクト

### 14.1 パーティクル・テキストエフェクト

| エフェクト | 説明 | トリガー |
|-----------|------|---------|
| damage_text | 赤い「-1」テキスト | 被弾 |
| damage_particle | 赤い粒子 | 被弾 |
| revival_text | 金色「+復活」テキスト | 復活 |
| revival_particle | 金色粒子 | 復活 |
| heart_particle | ハート粒子 | HP回復 |
| explosion_ring | オレンジの拡大リング | ボス撃破 |
| explosion_particle | 炎の粒子 | ボス撃破 |
| explosion_flash | 白いフラッシュ | ボス撃破 |
| combo_text | コンボ数テキスト | 2コンボ以上 |
| combo_spark | コンボ粒子 | 2コンボ以上 |
| combo_ring | 拡大リング | コンボ時 |
| boss_defeated_text | 金色「DEFEATED!」 | ボス撃破 |
| boss_shockwave | 楕円衝撃波 | ボス撃破 |
| score_text | 黄色スコアテキスト | スコア獲得 |

### 14.2 パワーアップオーラ

| パワーアップ | オーラ色 | 演出 |
|------------|---------|------|
| シールド | 青 | 魔法陣＋六角形紋章＋回転リング |
| レモン | 緑 | 風の柱＋上昇粒子＋渦巻き |
| エナジー | 赤 | 炎のオーラ＋回転リング＋十字紋章 |
| マグネット | 紫 | 楕円磁力線＋N/S極マーク |

### 14.3 背景演出
- パララックス（多重スクロール）: 山(0.15×)、木(0.25×)、雲(0.3×)
- バイオーム遷移のカラーグラデーション補間
- 夜バイオームの星空＋きらめき演出
- 画面シェイク（コンボ5+時）

---

## 15. データ永続化

### 15.1 localStorage保存データ

**`piyo_settings`キー:**
```
{
  dataVersion: number,         // セーブデータ構造バージョン（移行処理用、現在1）
  soundEnabled: boolean,       // サウンドON/OFF
  language: 'ja' | 'en',      // 言語設定
  adFree: boolean,             // 広告非表示
  savings: number,             // 貯金額
  upgrades: object,            // タイトルショップ購入済みアップグレード
  loginStreak: number,         // 連続ログイン日数(1-7)
  lastLoginDate: string,       // 最終ログイン日(YYYY-MM-DD)
  lastLoginTimestamp: number,  // 最終ログインタイムスタンプ
  loginBonusHistory: number[], // 受取履歴タイムスタンプ配列
  pendingStockItems: string[], // 未受取ストックアイテム
  tshopAdCooldown: number,     // 広告クールダウンタイムスタンプ
  purchased: object,           // 課金済み商品
  loginPassExpiry: number,     // ログボ強化パス有効期限
  ownedSkins: string[],        // 所持スキン一覧
  activeSkin: string           // 装備中スキン
}
```

### 15.2 Firebaseランキング
- Firebase Realtime Databaseを使用
- `scores` パスに保存
- 保存データ: name, score, distance, enemyKills, speedLevel, timestamp

---

## 16. PWA対応

| 機能 | 実装状況 |
|------|---------|
| Service Worker | `sw.js` で登録 |
| マニフェスト | `manifest.json` |
| アイコン | 192×192, 512×512 |
| オフライン対応 | キャッシュ戦略 |
| 強制更新 | `forceUpdate()` でキャッシュクリア＋SW解除 |
| 更新通知 | 新バージョン検出時にUIで通知 |

---

## 17. 課金要素（Stub実装）

現在は全てスタブ（ダミー）実装。Capacitor導入時にネイティブプラグインへ差し替え予定。

| 商品 | タイプ | 状態 |
|------|--------|------|
| スターターパック | 買い切り | Stub |
| 広告非表示 (Ad-Free) | 買い切り | Stub（UI非表示中） |
| ログボ強化パス | 期間限定（30日） | Stub |
| コインマスター | アップグレード | 「準備中」表示 |
| スキン | コスメティック | 未実装 |

---

## 18. デバッグモード

**有効化方法:** ポーズ画面のタイトル部分を3秒以内に10回タップ

**効果:**
- HP: 99に設定
- スコア: 50,000に設定
- 画面に「DEBUG MODE ON」と表示

**無効化:** 同じ操作で再度トグル（HP: 5、スコア: 0にリセット）

---

## 19. レスポンシブ対応

| 対応 | 内容 |
|------|------|
| Safe Area | iPhoneノッチ・ホームインジケータ対応（CSS env()） |
| 縦画面 | 「画面を横向きにしてください」メッセージ表示 |
| PC表示 | 600px以上でフォントサイズ・パディング固定 |
| タッチ最適化 | 最小タッチターゲット 44×44px |
| デッドゾーン | 中央37%を誤タップ防止領域として確保 |

---

## 20. gameRound制御一覧

`gameRound` は1から始まり、ボス撃破後のラウンド移行時に+1される。

| 影響項目 | 計算式 / 条件 |
|---------|-------------|
| ボス出現距離 | 2400 × gameRound (m) |
| ボスHP | 10 + max(0, gameRound - 3) × 3 |
| ステージBGM | (gameRound - 1) % 3 → 0:stage / 1:stage2 / 2:stage3 |
| 敵速度スケール | × (1 + (gameRound - 1) × 0.3) |
| ボス後スクロール速度 | savedSpeed × (1 + (gameRound - 1) × 0.2)、上限6.0px/f |
| 地上ミニオン（ボス戦） | ラウンド2以降、間隔 max(90, 180 - (gameRound - 2) × 20)F |
| 飛行ミニオン（ボス戦） | ラウンド3以降、間隔 max(120, 240 - (gameRound - 3) × 20)F |
| ボスアリーナアイテム | ラウンド3以降はドロップ停止 |
| 閃光アタック | ラウンド2以降で解禁 |

---

## 21. 素材リスト

### 音声ファイル
| ファイル | 素材元 | ライセンス |
|---------|--------|-----------|
| title.mp3 | NullPo Works (Suno生成) | 自社制作 |
| stage.mp3 | NullPo Works (Suno生成) | 自社制作 |
| stage2.mp3 | NullPo Works (Suno生成) | 自社制作 |
| stage3.mp3 | NullPo Works (Suno生成) | 自社制作 |
| boss.mp3 | NullPo Works (Suno生成) | 自社制作 |
| shop.mp3 | NullPo Works (Suno生成) | 自社制作 |
| gameover.mp3 | NullPo Works (Suno生成) | 自社制作 |
| ranking.mp3 | NullPo Works (Suno生成) | 自社制作 |
| win.mp3 | NullPo Works (Suno生成) | 自社制作 |
| warning.mp3 | NullPo Works (Suno生成) | 自社制作 |
| stage4.mp3 | NullPo Works (Suno生成) | 自社制作 |
| stage5.mp3 | NullPo Works (Suno生成) | 自社制作 |
| bonus.mp3 | NullPo Works (Suno生成) | 自社制作 |
| tutorial.mp3（元タイトル: Pixel Picnic Parade） | NullPo Works (Suno生成・2026-07-08・タグ/カバー画像除去済み) | 自社制作 |
| flash.mp3 | OtoLogic | CC BY 4.0 |
| or.mp3 | OtoLogic | CC BY 4.0 |
| select.mp3 | OtoLogic | CC BY 4.0 |
| piyoflash.mp3 | 作者確認済＝権利関係クリア（出所は失念・2026-07-17ユーザー確認） | 使用OK |
| piyoflash_charge.mp3 | 作者確認済＝権利関係クリア（出所は失念・2026-07-17ユーザー確認） | 使用OK |
| protect.mp3 | 作者確認済＝権利関係クリア（出所は失念・2026-07-17ユーザー確認） | 使用OK |
| critical.mp3 | 作者確認済＝権利関係クリア（出所は失念・2026-07-17ユーザー確認） | 使用OK |
| dokan.mp3（元ファイル名: arsnd_00019_dokan.mp3） | アルスパーク（アルスクール株式会社・arspark.jp） | 独自規約: 商用可・加工可・クレジット任意・**再配布禁止**（規約PDF 2023-07版で確認） |

> ⚠ アルスパーク規約の注意（2026-07-08確認・規約第4条(3)/第5条）: 素材の「再配布」は禁止で、
> 「作品を通じて第三者が素材をダウンロードできる状態」の公開は Scratch/アルスパーク上に限り許可と明記。
> **Web版（GitHub Pages）はmp3が生URLで取得可能なため、厳密にはこの状態に該当し得る（グレー）。**
> ネイティブアプリ（バンドル内蔵）は一般的な「作品への利用」解釈で問題になりにくい。
> → ストア提出前にアルスクール社へ「Webゲーム/スマホアプリのSEとしての利用可否」を確認するのが安全。

> OtoLogic分のクレジット表記は Ver.1.404 で設定画面に追加済み（効果音素材：OtoLogic（https://otologic.jp）/ CC BY 4.0）。
> piyoflash/piyoflash_charge/protect/critical の4ファイルは出所メモを失念したが、ユーザー（作者）が権利関係クリアと確認済（2026-07-17）。追加クレジット不要。

### スプライト
- プレイヤー、敵、ボス、地形、アイテム等は `sprites.js` で定義
- UIアイコンは `images/` ディレクトリに格納

---

*この仕様書はゲームコード Ver.1.271 に基づいて作成されたものです。*

## §13 敵の難度の伸び（1.715 で見直し）

⚠**発端**: ユーザー実機報告「R24でしおりに保存してあるが、この地点で雑魚のスピードはもうMaxか？
店や土管で回復もできるし、少なくとも私なら全然ゲームオーバーになる気配がない」
→ 実装を確認したところ **R24 以降は雑魚に関して一切難しくならない**状態だった:

| 要素 | 1.714までの実態 |
|---|---|
| 地上の雑魚の速度 | 7.0倍で頭打ち（**ちょうど R24 で到達**） |
| 飛行の雑魚の速度 | 4.0倍で頭打ち（R12 で到達） |
| 湧きの頻度 | **ラウンド無関係の固定値**（地上 90F+40% / 飛行 180F+15%） |
| 敵の種類の重み | **固定**（ひよこ2・金1・ママ1の抽選） |
| アカバネ率 | **R11以降ずっと 0.30 固定** |
| ボスHP | R13 で頭打ち（地底ボスは R21 で 300 頭打ち） |

⚠**速度に上限を入れた 1.628 のコメントは「難度は速さの代わりにアカバネ・敵の数・構成で上げる方針」と
書いてあるが、その代わりの上昇が実装されていなかった。** 上限だけ入って伸びが消えていた。

### 1.715 の変更（ユーザーが計算結果から選択）

| | 変更前 | 変更後 |
|---|---|---|
| `ENEMY_SPEED_MULT_CAP` | 7.0（R24到達） | **8.2（R29到達）** |
| アカバネ率 | 0.30 固定 | **`diveBirdRate()`＝R11で30% → 地上ラウンドごと約+1.30% → R38で60%頭打ち** |
| `ENEMY_SPEED_MULT_CAP_FLY` | 4.0（R12到達） | **据え置き** |

**実測値（ブラウザで検算済み）**

| R | 速度倍率 | アカバネ率 | アカバネの間隔 |
|---|---|---|---|
| R11 | 3.70 | 30.0% | 10.4秒 |
| R20 | 6.10 | 40.4% | 7.7秒 |
| **R24** | **7.00** | **44.3%** | **7.0秒** |
| R29 | **8.20**（頭打ち） | 49.6% | 6.3秒 |
| R38 | 8.20 | **60.0%**（頭打ち） | 5.2秒 |

### 決めた理由（変えない方がよい部分の記録）

- **8.2 の根拠＝踏み窓**。スクロール6.0px/f固定・踏み帯78px・地上雑魚の素の速度の加重平均1.4375px/f で計算すると
  7.0→4.86F、**8.2→4.39F**、9.4→4.00F。1.628 の「5Fを切ると狙って踏めない」に対し**目減りを0.47F(約10%)に収めた**。
  ⚠**9.4 以上は上げない**（4.00F＝限界）
- ⚠**飛行の上限は上げない**。素の速度が地上の約2倍(2.0〜4.0px/f)あり、1.626 の実測で7.0倍だと
  **接近0.30秒＝人間の単純反応(約0.25秒)とほぼ同じ**で避けようがなかった。
  **飛行は速度ではなく頻度で上げる**
- ⚠**アカバネ率の上限は 60%**。飛行の湧き自体(3.1秒に1体)は増やしていないので、率を上げるほど
  「ただ飛ぶだけの雑魚」が消えて全部アカバネになる。100%にすると空中が全部攻撃してくる別ゲームになる
- ⚠**到達を R38 まで引っぱるのが要点**（当初案の R21 到達は「早すぎる」でユーザー却下）。
  **速度の頭打ちが R29 なので、速度が止まった後もアカバネだけは増え続ける**＝難度の伸びが途切れない

### 次に手を入れるならここ（まだ固定のまま）

1. **飛行雑魚の湧き間隔 180F** を後半だけ詰める（＝1.628 が言っていた「数」で上げる）
2. **地上雑魚の湧き間隔 90F / 確率 0.4** も同様に固定
3. **種類の重み**（金ひよこ＝速い個体の比率）を後半だけ上げる
4. **回復側**（店の回復アイテムの価格・土管の回復部屋の出現率）は一切ラウンド依存でない

⚠いずれも**ランキングの不変条件（距離／速度／Lv=floor(距離/300)+1）には触れないこと**。

## §14 中断中（しおり）のタイトルショップ（1.716 で修正）

⚠**ユーザー実機報告**:「**中断してその間にタイトルショップへ行くと、実際に現在持っているアイテムと
全然違うアイテムが出てくる**」

### 原因

`interruptRunWithShiori()` は保存後に `showStartScreen()` を呼び、その中で **`resetGame()` が走る**。

```
buildPermaSlots();          // ポーチを permaStock から作り直す ⚠used が全部 false に戻る
stockState.items = [];      // 通常枠を空にする
flushPendingStockItems();   // ログボの品をここで配る
```

タイトルショップはこの**ライブの `stockState`** を表示していたので、画面に出ていたのは
**「次に『さいしょから』を選んだ場合の持ち物」**だった。

**実測（ブラウザで再現）**

| | perma | items |
|---|---|---|
| ラン中（＝しおりの中身） | `barrier(使用済)`, `full_charge` | `heal`, `lemon_special`, `shiori` |
| 中断後のライブ（バグ） | `barrier`, `full_charge` | （空） |

### ⚠表示だけの問題ではなかった（お金の複製）

売却対象の判定は「中身があり かつ **未使用**」（`stockSlotSellTarget`）。`used` が false に戻るため、
**ランで使い終えた永続品まで「未使用」として売れる**＝効果は使用済みのまま代金だけ得られる。
1.526 のポーチ転売・1.697 のポーチ品換金と同じ種類の穴。

⚠**しおり本体は壊れない**ことも確認済み: `canSaveRun()` が `gameStarted` を要求＝タイトルでは再保存されない。
`commitPermaStock()` はゲームオーバー時のみ。中断中のポーチ売買は 1.653 の `permaOwned` 突き合わせが吸収する。

### 対策（案1・ユーザー選択）

1. **中断中はしおりの中身を読み取り専用で表示する** — `tshopSuspendedView()` が
   `loadRunState()` から `{perma, items}` を作り、`updateStockUI(view)` に渡す。
   ⚠**ライブの `stockState` は絶対に触らない**（触ると `used` が戻り「しおりが復活して無限に中断できる」罠）。
   ⚠**枠の数は現在の `stockState.maxSlots` / `permaLevel()` で出す**（中断中にポーチを買えば枠は増える。
     しおりの中身は増えないので空の金枠が増える＝再開時に `applyRunSave` が長さを合わせる仕様と一致）
2. **中断中は売却を止める** — `tshopHasSellable()` が false を返し、メニューを押すと
   `tshop_sell_suspended`（「ぼうけんの とちゅうでは かいとりできないのだ」）を出す。
   **買うのは従来どおり可**（`permaOwned` の突き合わせがあるため整合が取れる）
3. ⚠**タイトルショップ内でストック表示を更新する時は必ず `refreshTshopStockUI()` を通す**。
   `updateStockUI()` を素で呼ぶとライブが出て元に戻る（売却後・ポーチ購入後の2箇所が該当した）

### 検証（ブラウザ・1.716）

| 確認項目 | 結果 |
|---|---|
| 中断中の表示がしおりと一致 | ✅ 金枠1=使用済(空)／金枠2=full_charge／通常=heal, lemon_special, shiori／残り空 |
| 中断中は売れない | ✅ `tshopHasSellable()`=false・押すと「ぼうけんの とちゅうでは かいとりできないのだ」 |
| 通常時（しおり無し）は従来どおり | ✅ ライブを表示・`tshopHasSellable()`=true・押すと売却モードへ遷移 |
| コンソールエラー | ✅ なし |

---

## §15 中断中（しおり）に地底モードを始めさせない（1.717 で修正）

⚠**ユーザー実機報告（2026-08-04）**:「中断中に地底モードをスタートしたら、今持っているものと
全く違うアイテム欄になった」。§14（中断中のタイトルショップ）と**まったく同じ構造**の不具合。

### 原因

`startUndergroundMode()` が**しおりの存在を一切見ずに**新しいランを始めていた。

- 「さいしょから」は `startNewRunFromMenu()` が **確認ダイアログ → `clearRunState()` → `startGame()`** を通る
- **地底モードだけがその入口を持っていなかった** ＝ 中断したランと地底モードのランが**同時に存在**する

### 開いていた穴（4件）

| | 内容 |
|---|---|
| ① 表示 | 持ち物欄が「しおりの中身」ではなく `permaStock` から作り直した**未使用の金枠**＋ログボ配布品になる |
| ② 消耗品の使い回し | 中断前に使った金枠が `used:false` で戻る。地底モードは `commitPermaStock` が**クリア時しか走らない**（1.696）ので `permaStock` は無傷 → 再開すると元通り＝**同じ消耗品を何度でも使える** |
| ③ ログボ品の消失 | `resetGame` → `flushPendingStockItems()` が `pendingStockItems` を空にして保存する。しおりを再開する `continueRunFromSave()` は「配り直すために退避」するが、もう空 |
| ④ ポーチの書き換え | ③の中の `commitPermaStock(**true**)` は force なので**地底モードの除外ガードを迂回**し `permaStock` を上書き＝再開時の `permaOwned` 突き合わせ（1.653）が誤作動しうる |

### 決めたこと（案A・✅ユーザー決定 2026-08-04）

**中断中は地底モードを始められない。** 押すと理由を出す（`menu_ug_mode_suspended`）:
> しおりを はさんだ ぼうけんが のこっています。「つづきから」か「さいしょから」を さきに えらんでください。

⚠**黙って弾かない**（グレーアウトも含む）＝「押しても反応しない不具合」に見えるため。§14 の売却停止と同じ扱い。

**この案を選んだ理由**: 根っこは**「ランは同時に1本」という前提が地底モードの入口だけ守られていない**こと。
案Aは前提を戻すだけで新しい規則を増やさず、①〜④が入口1箇所で同時に消える。しおりも消えない。
- 却下した案B（さいしょからと同じく しおりを消す）＝寄り道のために冒険が消えるのは代償が釣り合わない
- 却下した案C（しおりを保ったまま両立させる）＝`commitPermaStock` を完全に止める必要があり、
  **「クリアすれば老婆の劇薬を持ち帰れる」仕様（1.696）と衝突**する。②のような抜け道も塞ぎ切れない

### 他の入口の監査（1.717 で実施）

ランを始める経路は3つだけ（`gameState.gameStarted = true` の代入箇所で確認）:

| 経路 | 判定 |
|---|---|
| `startGame()`（さいしょから） | ✅安全。`startNewRunFromMenu` が確認＋`clearRunState()` を通す |
| `continueRunFromSave()`（つづきから） | ✅安全。読み込んだ直後に `clearRunState()` |
| `startUndergroundMode()`（地底モード） | ❌**これが穴だった** → 1.717 で門を追加 |

⚠**チュートリアルは `startGame()` を直接呼ぶ**（`tutorialBegin` / 設定の「チュートリアルをあそぶ」）が、
サンドボックス判定で①〜④が起きない＝門は不要:
`buildPermaSlots` が**0枠**／`flushPendingStockItems` が即return／`commitPermaStock` が即return。
また `canSaveRun()` がチュートリアルと地底モードを弾くので、**そこから2本目のしおりは作れない**。

### 検証（ブラウザ・1.717）

| 確認項目 | 結果 |
|---|---|
| 中断中に地底モードを押す（実際のタップ操作） | ✅ 弾く。モーダルに理由が出る／`gameStarted=false`／`undergroundMode.active=false` |
| その時しおりが無傷か | ✅ `hasRunSave()=true` のまま |
| その時 `permaStock` が無傷か | ✅ `['barrier','lemon']` のまま |
| 弾いた後に「つづきから」で再開できるか | ✅ 金枠1=使用済(空)／金枠2=lemon／通常枠=shield と**しおりどおり**に復元。しおりは消える |
| しおりが無い時は従来どおり始まるか | ✅ `undergroundMode.active=true` stage=1／`gameRound=7`／路銀30,000 |
| コンソールエラー | ✅ なし |

⚠実機テストプレイは未了（ブラウザでの実測のみ）。
