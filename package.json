{
  "name": "piyos-adventure",
  "version": "1.0.0",
  "private": true,
  "description": "ぴよ氏の冒険 (Piyo's Adventure) — Capacitor native wrapper",
  "scripts": {
    "build:web": "node scripts/build-www.mjs",
    "sync": "npm run build:web && cap sync",
    "sync:ios": "npm run build:web && cap sync ios",
    "open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor-community/admob": "^8.0.0",
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  }
}
