# RELEASE 1.7.4（2026-08-21）

⚠手順は `RELEASE_PLAYBOOK.md` が正。ここは**この版の実測値と提出の記録**。

| | 前回（1.7.3） | 今回（1.7.4） |
|---|---|---|
| 同梱ゲーム | Ver.1.722 | **Ver.1.723** |
| iOS | 1.7.3 / build 16 | **1.7.4 / build 17** |
| Android | 1.7.3 / versionCode 15 | **1.7.4 / versionCode 16** |

## 版数の判定

**0.0.1 上げ（1.7.3 → 1.7.4）**。中身は**既存仕様の調整1件のみ**で、遊べるものは増えていない。
内部番号は上げ幅に関係なく +1。

## 中身（Ver.1.723・1件）

- **ふっかつマシーンの復活を HP1 → HP2 に**（ユーザー指示）
  - ⚠ユーザー報告は「ふっかつやくで復活するとHPが1」だったが、**実体は名前の似た別物**。
    **ふっかつやく（`revive_potion`）は通常枠／ポーチの金枠／落下死／地底のいずれの経路でも
    既に HP2 で復活している**ことを実測で確認済み（説明文も元から「ライフ2で ふっかつする」）。
    HP1 で復活していたのはタイトルショップの永続アップグレード
    **「ふっかつマシーン」（`revival_feather`・50万/100万円）** だけだったので、こちらを HP2 へ揃えた。
  - 変更は `index.html`（`tryRevive` の `lives = 1` → `2`）と `i18n.js`（ja/en の説明文）。

### 検証（実物のコードをブラウザ上で回した）

| 経路 | 結果 |
|---|---|
| ふっかつやく（通常枠／ポーチ／落下死／地底） | **すべて HP2**（修正前から） |
| ふっかつマシーン Lv1 | **HP2**（修正前は HP1） |
| ふっかつマシーン Lv2（2回ぶん） | **2回とも HP2**・残回数も正しく減る |
| どちらも無し | ゲームオーバー（従来どおり） |

優先順（薬 → マシーン）も維持を確認。

## 軽い回帰チェック（新規のバグは検出されず）

| 検査 | 内容 | 結果 |
|---|---|---|
| 連続プレイ | 地上をボス撃破しながら **R7 / 12,000m** まで（数万フレーム） | 例外 0・NaN 0 |
| 地底 | 入場して 45,000フレーム・部屋0〜3を踏破 | 例外 0 |
| 配列の膨張 | エフェクト／敵／地形／足場／コイン／弾の同時数を追跡 | すべて上限内・**漏れなし** |
| 多言語 | ja/en のキー突合 | **631/631 一致**・欠落0・英語への日本語混入0・プレースホルダ不一致0 |
| 英語UI | `setLanguage('en')` で 3,000フレーム＋UI更新 | 例外 0 |
| コンソール | 起動〜プレイ中 | エラー 0 |

⚠検査中に「エフェクトが1,963件まで溜まる／ボス戦中に刈られない」と見えたが、**どちらもテストの作り方の問題**。
スプラッシュ画面が出たままだと `render()` が早期 return し、エフェクトの寿命を進める `frameSteps` も 0 のまま。
正規の手順（タップ→`startGame()`）で開始し直すと正常に刈られる。**次回この検査をする時は必ずスプラッシュを消してから**。

## 成果物の実測

### Android（AAB）
```
android/app/build/outputs/bundle/release/app-release.aab
```
| 項目 | 実測 |
|---|---|
| サイズ | **60.63 MB**（63,579,334 bytes・前回 63,579,251 bytes ＝ **+83 bytes**） |
| 同梱ゲーム | Ver.1.723 |
| versionName / versionCode | **1.7.4 / 16**（protobuf マニフェストから実測・旧 1.7.3 の残留なし） |
| 署名 | `META-INF/UPLOAD.RSA` あり |
| 混入 | **なし**（public 直下は js 8本＋cordova 2本＋images＋sounds＋index.html） |
| 修正コード | AAB 内に 1.723 の修正＋1.722 の修正の**両方を確認** |

### iOS（xcarchive）
```
~/Library/Developer/Xcode/Archives/2026-08-21/PiyosAdventure-1.7.4-build17.xcarchive
```
| 項目 | 実測 |
|---|---|
| CFBundleShortVersionString | **1.7.4** |
| CFBundleVersion | **17** |
| 同梱ゲーム | Ver.1.723 |
| App.app | 60 MB |
| 混入 | **なし**（public 直下は Android と同一） |
| 結果 | **ARCHIVE SUCCEEDED** |

## リリースノート（両ストアへ入れる文面）

### 日本語
```
【調整】
・「ふっかつマシーン」で復活したときのライフを 1 から 2 に増やしました。「ふっかつやく」と同じ強さになります。
```

### English
```
Changes
- Reviving with the Revival Feather now restores 2 life instead of 1, matching the Revival Potion.
```

## プロモーション用テキスト（ASC・売り文句／不具合修正は書かない）

⚠`RELEASE_PLAYBOOK.md` §6 の🚫を参照。1.7.3 と同じ文面を使い回す。

```
ja: かわいい「ぴよ氏」と、どこまでも走る横スクロールアクション！ジャンプでかわして敵を倒し、個性豊かなボスに挑もう。きせかえ・図鑑・ボーナスステージ、そして地底へ。世界ランキングで自己ベストを更新しよう！
en: Run as far as you can with the adorable Piyo! Dodge, stomp and face a cast of bosses. Costumes, bonus stages and the underground await.
```

## 提出の記録（2026-08-21）

### ⚠前提の確認
Play のトラック画面で **`15 (1.7.3)` が「8月12日 21:38 に公開」** ＝前版は公開済みを実査。
（[[piyo-latest-is-released]] の方針どおり、審査中を疑わず素直に 0.0.1 上げた判断は正しかった）

### Play（クローズドテスト Alpha）— **✅送信完了**
- AAB `16 (1.7.4)` をアップロード（アップロードボタン→ファイルダイアログ・D&Dは使わない）
- リリースノートを `<ja-JP>`/`<en-US>` 併記 → **「リリースノート: 2 言語」**を確認
  ⚠入力直後の `document.body.innerText` は **「0 言語」と出る**（DOM 反映が1テンポ遅れる）。
  **もう一度読み直すと「2 言語」になる**＝ここで慌てて入れ直さないこと
- レビュー画面の実測: **新規インストール 62.3MB（前リリース差 +47 B）／アップデート 3.12MB／
  ダウンロード時間 37秒（差 0秒）**・サポート対象デバイスの増減なし
- 警告は1件のみ＝**難読化解除ファイルが無い**（バージョンコード 16・`minifyEnabled false` なので該当なし）
- 段階的な公開の割合は既定の **100.0**（触っていない）
- 保存 → 「概要に移動」→ 「1 件の変更を審査に送信」→「変更を審査に送信」 →
  見出しが**「審査中の変更」**に変化＝完了

### ASC（iOS 1.7.4 / build 17）
（提出しだい追記）

## ⚠この版の未了事項

- **実機テストプレイは 1.697〜1.723 が丸ごと未了**（ブラウザでの実測のみ）
- **Android 実機で1回だけ確かめたいこと**＝「戻るキーで popstate が発火するか」（引き継ぎオーバーレイの履歴ズレが未対応）
- 監査の完全性チェックが挙げた「次に見るべき場所」（`audio.js` の音の状態機械／外部CDN依存の
  フォントと Firebase／端末のフォント拡大設定／時刻の扱い全般）は**手つかず**
