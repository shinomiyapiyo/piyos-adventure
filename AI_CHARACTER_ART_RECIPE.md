# AI で「同一人物」のゲーム用キャラ絵とモーションを作る手順書

⚠**他のプロジェクトへ持ち込むための独立文書**。「ぴよ氏の冒険」で 2026-07〜08 に一枚絵15枚と
きせかえ／敵／ボスのモーションを作った実績から、**実際に失敗して分かったこと**だけを書いた。
特定のゲームに依存する数値は「例」と明記してある。そのまま真似せず、自分のゲームで測り直すこと。

使うもの:
- **静止画＝Gemini の画像モデル（通称 Nano Banana）** `gemini-3-pro-image`（API キー `GEMINI_API_KEY`）
- **動きのコマ＝Veo 3.1** `veo-3.1-fast-generate-preview`（image-to-video）
- 加工＝`sharp`（Node）、フレーム抽出＝`ffmpeg`

---

# 第1部 静止画（一枚絵・カットイン・立ち絵）

## 1. まず「正（せい）」の文書を作る。これが無いと毎回ブレる

キャラの見た目を**1枚の基準画像＋文章**で固定し、生成のたびに1項目ずつ突き合わせる。
文章には**色を16進で**書く。「黒髪」ではなく「純黒ではなく紫みを帯びた暗いチャコール `#3A313F`（影 `#251C20`）」。
生成AIは「黒」と書くと純黒にし、「紫っぽい黒」と書くと紫にしすぎる。**数値だけがブレない。**

正の文書に必ず入れる項目:

- 髪（色・形・長さ・前髪の形）／目（形・虹彩の色・ハイライトの形と数）／肌の色
- 衣装のパーツを**上から下まで漏れなく**（襟・リボン・袖・胴・スカート・靴の飾りまで）
- **無いもの**（例: 尻尾は無い／ネコ耳は無い）← ⚠これが一番効く。後述
- 配色の一覧（差し色が何色までか。「黄と黒の2色構成＋白・金・ピンクだけ」など）
- 描き方の癖（リムライトの有無、輪郭線の太さ、影のグラデーションの入れ方）

## 2. 判定の優先順位を先に決める（ユーザーの体感はここで決まる）

実際に運用して確定した順位。**上から順に見る。**

1. **画風（タッチ）が他の採用絵と同じか**
2. **同一人物に見えるか**
3. **同一年齢に見えるか** ← ⚠**一番見落とす。後述**
4. 衣装・小物の正しさ
5. ドットの粒の細かさなどの数値 ← **一番下**

⚠**数値を測って安心してはいけない。** 数値が基準内でも「幼く見える」「のっぺりしている」で却下される。
逆に数値が基準外でも1〜3が合っていれば採用される。**必ず採用済みの絵と並べてから見せる。**

## 3. 参照画像の渡し方 ＝ この手順書の核心

Gemini は**渡した画像に強く引っぱられる**。これを利用も制御もできる。

### ✅ 効くパターン A: 「これを維持して、○○だけ変えろ」

**直したい絵そのものを参照に渡す。** 構図・部屋・ポーズ・色が保たれ、指定した1〜2点だけ変わる。

```
REFERENCE IMAGE 1 IS THE ARTWORK TO REPRODUCE. Redraw THE SAME PICTURE and keep everything identical:
the same framing, the same pose, the same background, the same face and expression, the same colours
and the same pixel-art block size.
CHANGE EXACTLY ONE THING: <変えたいこと>.
Nothing else in the picture changes.
```

これで成功した実例: 髪の色だけ直す／肩にひよこを1羽足す／表情だけ嬉しそうにする／
ノイズだけ消す／髪の長さだけ伸ばす。

### ❌ 効かないパターン: 「旧版を渡して顔を直せ」

**顔・年齢・髪型を変えたい時に旧版を渡すと、旧版のそれをそのまま引き継ぐ。**
「顔だけ直して」と書いても効かない。3枚生成して3枚とも元の顔のままだった。

### ✅ 効くパターン B: 「旧版は渡さない。新しく描かせる」

顔や年齢を変えたい時は**旧版を参照から外す**。代わりに:

1. **正の顔を切り出して拡大した参照**を作る（例: 採用済みの絵から顔を 680px に切り出す）
2. 参照は **①その顔** ＋ **②部屋・画風の見本** の2枚だけ
3. 場面・芝居・衣装は**言葉で組み立て直す**

⚠**この方法は髪型と髪飾りが飛ぶ。** 実例: 3案のうち2案で**ツインテールと髪飾りが消えて別人**になった。
**衣装と髪型の指定を毎回入れて、上がりを必ず全部見る。**

### 参照の役割を明示的に切り分ける

複数枚渡す時は、どの参照から何を取るかを文章で指定する。これは効く。

```
REFERENCE IMAGE 1 IS THE TRUTH FOR HER FACE, AGE AND OUTFIT.
REFERENCE IMAGE 2 DEFINES THE ROOM AND THE ART STYLE ONLY.
⚠IGNORE THE GIRL IN REFERENCE IMAGE 2 COMPLETELY — her face is wrong, which is what we are fixing.
```

## 4. 「同一年齢」の作り方（最も見落とす）

「可愛く」「大きな目で」を効かせすぎると**幼児になる**。実際に「幼すぎる」で却下された。
**年齢の正となる絵を1枚決めて、顔幅を揃えて並べて比べる。**

幼く見える症状は毎回この6つ:

| | ❌幼く見える | ✅ |
|---|---|---|
| 目の形 | 真円に近い大きな目 | **横長の楕円**でやや小さい |
| 目の位置 | 顔の中央より**下** | 顔の中央〜やや上 |
| 目の間隔 | **広い** | やや狭い |
| 目からあご | **短い** | **長い**（あごとあごのラインが見える） |
| チーク | **大きく横に広い** | 小さい楕円 |
| 顔の輪郭 | 真円寄り | やや縦長 |

英語プロンプトの型:

```
Her eyes are ALMOND-SHAPED and WIDER THAN THEY ARE TALL — not big perfect circles;
they sit at the MIDDLE of her face, with a NARROW gap between them;
the distance from her eyes down to her chin is LONG, giving a clear jaw and chin;
her face is a SLIGHTLY TALL OVAL, not a circle; her blush is a SMALL neat oval on each cheek.
She reads as a girl of about twelve to fourteen — NOT a small child, NOT a toddler.
```

⚠**全身が写る絵は頭身で測れる**（あごから足元が頭の高さの3個分以上あるか）。
**胸から上の絵は頭身が測れない**ので「肩幅 ÷ 顔幅（肌の幅）」で見る。
基準画像で実測して自分の比を出しておく（例のプロジェクトでは 4.71・3.4未満はデフォルメ判定）。

## 5. 「無いもの」を毎回明示的に禁止する

生成AIは**ある方に寄せる**。無いものは書かないと必ず生える。

```
NO CAT TAIL — she does not have a tail at all. Do not draw a tail behind her or beside her legs.
NO bare shoulders. NO large bow at her waist. NO high heels — flat rounded shoes only.
NO flower or rose on her chest. NO props in her hands.
```

⚠**指示していない小物を持たせてくる。** 実例: 店の中の絵で**ハンマーを持って**上がってきて却下された
（「店の中でハンマーを持っているのは状況として意味不明」）。手元は必ず見る。
小物を持たせるなら**その場面を説明するもの**にする（例: 払えない場面で**空の巾着を逆さに振る**＝一目で伝わる）。

## 6. 参照絵の「動きに由来する形」を真似させない

⚠**参照にした絵がジャンプ中だと、立ち絵なのに髪だけ外に跳ねる。**

参照は**顔・年齢・衣装・髪の長さ**のために渡すもので、**ポーズと慣性の形は別物**として切り離す。

```
The tails HANG STRAIGHT DOWN under their own weight, close to her body, tapering to a point.
DO NOT make them flare, sweep or fan outwards — the outward flare in reference image 1 only happens
because she is jumping there; in THIS picture she is standing still.
```

同じ罠は**スカートの広がり・マントのはためき・服のしわ**でも起きる。

## 7. ドットの粒（ピクセルアート風の粗さ）は生成では制御できない

⚠**これは重要。** 「横に320粒の格子で描け」「ブロックを半分の大きさに」と指示しても、
Gemini は**自分の相対的な格子で描く**。2回試して2回とも効かなかった。

**画面上のドットの大きさを決める式:**

```
画面上のドットの大きさ ＝ (1 ÷ 絵の横に並ぶ粒数) × 表示される幅
```

⚠**ファイルの解像度を上げても変わらない。** 解像度を上げると表示倍率が下がって**完全に相殺される**
（1024→1365 にして実測し、画面上のドットが1画素も小さくならなかった）。

効く手は2つだけ:

1. **構図を引く**（同じ幅により多くの物を描く＝粒数が増える）。⚠寄りの絵では使えない
2. **硬い四角の段差そのものを消す**＝**なめらか化**（メディアン→ごく弱いブラー）。
   ドット絵の質感は捨てることになるが、写真的な寄り絵なら違和感が出ない
   ⚠なめらか化すると **PNG は1MB超に膨れる**ので **JPEG にする**（透過が不要なら）

**粒の測り方**（本物のドット絵ではなく「ドットを模した絵」なので格子は焼かれていない）:
「色が同じとみなせる画素の連続長」を粒として測る。⚠**必ずキャラ部分だけを測る**。
背景は平らな面が広く、粒が実際より大きく出る。
⚠**目のアップのような寄りの絵ではこの指標が壊れる**（白目や肌の平らな面の広さを測ってしまい、
新しい方が「粗い」と誤判定される）。**寄りの絵は等倍で並べて目で見る。**

## 8. 色ズレは描き直さず「色相だけ動かす」

⚠**立ち絵だけ色が違う**ようなケースは、**描き直してはいけない**（別物になる）。

実例: 立ち絵 `#f5a022`（色相36）／歩行・ジャンプ・落下は全コマ色相45。
**R と B は同じで G だけ33低い**だけだった。式1本で直る。

```
G' = B + (目標色相 / 60) × (R - B)      // R と B は触らない＝明るさと陰影の構造が保たれる
```

対象画素の絞り方（**衣装だけに当てる**）:

```
色相 25〜50 ／ 最大成分 ≧ 110 ／ B < 140 ／ 彩度 ≧ 0.4
```

これで実測**肌 0px・帯とブーツと輪郭 0px**＝衣装だけに当たった。
⚠**他コマと同じ数値に合わせると「黄色すぎ」になることがある。**
理由＝**立ち絵は平らな面が581px、歩行は10px**しかなく、**面が広いほど同じ色相でも強く出る**。
**数値を合わせるのではなく、段階を3つ並べて目で選ぶ。**（実例では正解45に対し39が採用された）

## 9. 出す時の作法

- **生成したら必ずそのターンで全候補を見せる。** 文章で「3枚できました」と説明するだけでは判断できない
- 検品（頭身・粒・色）は**見せる前**に済ませる。ただし検品を理由に見せるのを次に回さない
- **採用済みの絵と並べた比較画像**を一緒に出す（顔幅を揃えて並べると年齢の差が一目で分かる）
- リモート（スマホ）から見てもらう時は、**画像を data URI で埋め込んだ1枚のHTML**にして共有する
  （添付が開けない環境がある。外部URLはCSPで塞がれることがあるので必ず埋め込む）

## 10. 段階を全部残す

生成スクリプトは**画像1枚につき1本**残し、やり直しは `--set=v2` `--set=v3` のように**段階を足す**。
消さない。「どの指摘で何を変えたか」が追え、同じ手戻りを繰り返さなくなる。
⚠実例では1枚の絵に**指摘が4回**入った（幼い→外跳ね禁止→髪の長さと芝居→立ち食いに見える／のっぺり／寄れ）。
**1つ直すと次が見える**ので、最初から全部聞き出すのは無理。段階を残す方が早い。

---

# 第2部 動きのコマ（歩行・ジャンプ・攻撃）

## 11. ⚠動きのコマを1枚ずつ独立に生成してはいけない

**立ち絵とは別に「歩行1」「歩行2」を個別生成すると、コマ間で体格・服・色が変わってモーションが崩れる。**
必ず**動画を1本作ってからコマを切り出す**。同じ動画の中なら見た目が保たれる。

## 12. 手順

### ① アンカー（種画像）を作る
立ち絵を Gemini で確定させる。これが動画の入力になるので、**ここで妥協しない**。

### ② 緑背景の種フレームを組む
アンカーを**単色グリーンの縦長キャンバス**に合成する。後でクロマキーするため。

```
キャンバス 720×1280（9:16）／背景 rgb(0,200,0)／足元を y=1185 に合わせる
キャラは trim して 640×1000 に収め、水平中央に置く
```

### ③ Veo で image-to-video（1本ずつ）

```js
model: 'veo-3.1-fast-generate-preview'   // 詰める時だけ 'veo-3.1-generate-preview'
seconds: 4
image: <緑背景の種フレーム>
```

プロンプトに必ず入れる要素:

```
... walks in place in a smooth, clear SIDE-VIEW walking cycle, facing right.
Her legs clearly step: one foot forward, then both legs pass close together between steps,
then the other foot forward — a natural open-close-open walk.
She stays CENTERED in the frame and does NOT travel across the screen.
Keep her exact appearance, outfit, colors and pixel-art style identical to the input image.
Plain flat solid green background, no other objects, no camera movement, no zoom, no panning.
```

ネガティブ:

```
walking out of frame, background change, camera motion, panning, zoom,
extra characters, text, watermark, blur, realistic 3d render, out of frame
```

⚠**「歩く」と書くと画面外へ歩いて行く。** `walks in place` と `stays CENTERED` と
`does NOT travel across the screen` の3つを全部書く。
⚠**「両脚が近づく瞬間」を明示する。** 書かないと脚が開いたままの2コマしか出ず、歩いて見えない。

⚠**必ず1本ずつ生成して、都度見せて判断を仰ぐ。** 連続生成はクレジットを浪費する。

### ④ 全コマを ffmpeg で抽出

```bash
ffmpeg -i _raw/veo_<name>_walk.mp4 _raw/veo_frames_<name>/f_%03d.png
```

### ⑤ コンタクトシートで使うコマを選ぶ

全コマを番号付きのグリッド1枚に並べて、**目で見て採用コマを決める**。
歩行4コマなら「前・中間・後・中間」に当たる番号を拾う（実例: f_27/33/38/44、f_38/44/50/56）。

### ⑥ クロマキー＋整列してスプライトにする

ここが品質を決める。**単純な緑抜きでは輪郭に緑が残る。**

```
1. 緑度 = G - max(R,B) を計算する
2. 緑度 > 50 かつ G > 85          → 完全に透過（背景）
3. 緑度 18〜50                    → デスピル（G を max(R,B) に下げて緑かぶりを消す）＋
                                    緑度に応じて半透明にする（輪郭のなじませ）
4. アルファでバウンディングボックスを取る
5. **立ち絵の身長と足元の位置に合わせて**拡縮・配置して出力サイズへ（例: 64×64）
```

⚠**⑤の「立ち絵に合わせる」を省くと、歩き出した瞬間に身長が変わって見える。**
足元（接地線）と全高の2つを立ち絵に合わせること。

⚠Veo の出力は**立ち絵より彩度や明度がずれることがある**。ずれたら第1部の8番（色相の式）で合わせる。
コマ間で揃えるのが目的なので、**全コマに同じ補正を当てる**。

## 13. ボスなど多アクションの場合

1本の動画に複数の動作を詰めず、**アクションごとに1本**（idle / 攻撃 / 被弾 / 突進）。
出力先ディレクトリもアクションごとに分ける。採用コマは番号で指定できるようにする。

```bash
node veo-frames-to-<name>.mjs --idle=161 --flap=173 --dive=37 --shoot=117 --damaged=109 --preview
```

⚠`--preview` のように**本番の images/ を上書きせず確認用だけ出すフラグ**を付ける。
コマ選びは必ず外すので、上書きしてから気付くと戻せない。

---

# 第3部 まとめ（これだけは覚えておく）

1. **正の文書を作る。色は16進で書く。**
2. **判定は 画風＞同一人物＞同一年齢＞衣装＞数値。** 数値は最下位
3. **維持したいなら旧版を参照に渡す。変えたいなら渡さない。**
4. **「無いもの」を毎回禁止する。** 尻尾も小物も、書かないと生える
5. **参照の動きに由来する形（外跳ね・広がり）は真似させない**
6. **ドットの粗さは生成では変えられない。** 構図を引くか、段差を消すかの2択
7. **色ズレは描き直さず色相を動かす。** ただし数値ではなく目で選ぶ
8. **動きのコマは必ず動画から切り出す。** 独立生成は崩れる
9. **生成したらそのターンで全部見せる。比較画像も一緒に**
10. **段階を全部残す。** 指摘は1回で終わらない
