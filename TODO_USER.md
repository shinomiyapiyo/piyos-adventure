# ユーザー（白柳）が手を動かす必要があるタスク一覧

最終更新: 2026-07-29 / Ver.1.655 時点
Claude が代行できないもの＝**実機・管理画面・本人確認・判断**が要るものだけを挙げる。
コードで片が付くものはここには載せない。

---

## 🔴 いま止まっているもの（着手待ち）

### 1. iOS 1.5（ビルド10）の審査結果を確認する
- **状態**: 2026-07-28 に「審査待ち」で提出済み。一晩明けた時点で未確認。
- **なぜ本人か**: ASC のセッションが切れている（`authResult=FAILED`）。**再ログインが必要**。
- **手順**: ブラウザペインで App Store Connect にサインイン → Claude に「ログインした」と伝える。
  以降の確認・対応は Claude が代行できる。
- **通っていた場合**: 自動リリース設定なので、そのまま公開される。
- **リジェクトされた場合**: 理由を Claude に見せれば対応案を出せる。

### 2. 実機テストプレイ（1.650〜1.655 の5件が未確認）
- **なぜ本人か**: [[実機のみ]] という厳命。ブラウザ/シミュレータは代替にならない。
- Claude の検証は**すべて数値どまり**。手触りと実際の表示は見ていない。

| 版 | 確認してほしいこと |
|---|---|
| 1.650 | 闇のタマゴ(R3)を**画面左端で**踏む → 殻の外へ弾かれ、居座れないか。弾かれ方に違和感がないか |
| 1.651 | しおりをタップ → 確認ダイアログが出るか。「いいえ」でちゃんと冒険に戻るか |
| 1.652 | バリアを使った直後に中断 → 再開して**バリアがまだ効いているか** |
| 1.652 | ボーナス土管を使ったラウンドで中断 → 再開して**入れない土管が立っていないか** |
| 1.653 | 中断 → タイトルショップでポーチを売る → 再開 → ラン終了で**品が戻ってこないか** |

- 強さの調整が要る場合、1.650 は `velX=±9` と `knockbackTimer=16`（gameplay.js）の数値だけで変えられる。

### 3. AdMob 管理画面で GDPR メッセージを作成する
- **なぜ本人か**: AdMob アカウントの管理画面操作。
- **これをやらないと**: 1.655 で入れた同意フォームが**一度も表示されない**
  （`isConsentFormAvailable` が false のまま＝EEA/UK では広告が出ない状態が続く）。
- **手順**: AdMob → プライバシーとメッセージ → **GDPR** → メッセージを作成 → 対象アプリに「ぴよ氏の冒険」を選択 → 公開。
  - 同じ画面に「米国州法」もあるが、そちらは別途（今回のコードは GDPR のみ対応）。
- **終わったら**: Claude に伝えれば、`UMP_DEBUG_EEA = true` にして実機で EEA を模擬する手順を案内する。

### 4. UMP の実機確認（3 の完了後）
- `monetization.js` の `UMP_DEBUG_EEA` を true にする（Claude が切り替える）
- 実機で起動 → 同意フォームが出るか／拒否しても**ゲームが普通に遊べる**か／
  設定に「広告のプライバシー設定」が出るか／そこから同意を変更できるか
- 確認後、**必ず `UMP_DEBUG_EEA = false` に戻す**（Claude が戻す）

---

## 🟡 期限はないが、いずれ必要なもの

### 5. Android 1.5 (vc9) の審査結果 → 確認済み
2026-07-29 時点で**審査を通過してクローズドテスト Alpha に公開済み**。対応不要。

### 6. 広告後の無音を調べる
- **なぜ本人か**: ネイティブ層のログが要る＝実機を Mac につないで Xcode/Logcat を見る必要がある。
- 症状: 広告を見た後にゲームの音が戻らないことがある。
- Claude 側は `recoverGameAudio()` を入れて対処済みだが、根治していない。
- **必要なもの**: 再現時の実機ログ。取れれば Claude が原因を特定できる。

### 7. 1周の実測（テンポの確認）
- **なぜ本人か**: 実際に遊んで時間を計る必要がある。
- 1周（地上6ラウンド＋地底1）が**何分かかるか**をストップウォッチで1回計るだけでよい。
- **なぜ重要か**: ランナーは「もう1回」が押せる短さが命。ここが長いなら、アートやオンボーディングより
  先にテンポ調整をやるべきで、**改善の優先順位が丸ごと変わる**。今この数字が無いまま議論している。

### 8. DUNS 取得 →（取得後）iOS/Android を組織アカウントへ切り替え
- **状態**: 遠征のため先送り中。
- 個人名義だと住所が公開されるため、取得後に両ストアとも切り替える方針。
- ⚠**iOS は既に個人名義で公開中**（1.4.2 配信中）。公開を止めるか切替まで待つかは判断が要る。
- 切替時は iOS＝Apple Developer Program の組織変更、Android＝Play Console のアカウント種別で
  手順が別なので、目処が立った時点で改めて調べる。

---

## 🟢 判断だけしてもらえればよいもの（作業は Claude）

### 9. 6.9インチ スクショ10枚目の追加
- `スクショ用/ASC_6.9inch/10_ug_priestess.png`（2868×1320）を出力済み。
- iOS 1.5 が**審査待ちの間は差し替えできない**ので、承認後に追加する。
- 日本語は現在 9/10 なので、ちょうど1枚ぶん空きがある。

### 10. オンボーディングの段階開示をやるか
- 図鑑・バッジ・ポーチ・エッグ交換・店3種を初回に全部見せている。
- 「2回目にポーチ、3回目に図鑑」のように開示を分けたい。**開示順の判断**をもらえれば実装できる。

### 11. 新要素をやるか（Claude が設計書を出せる）
- 地底モードの持ち帰り報酬（現状クリア報酬はバッジ1つだけ＝30万円の対価として弱い）
- ランキングに腕前が出る部門（現状は距離のみ＝長く走れた人が勝つ）
- 中盤（数十万円帯）の目標

### 12. アートのトーン統一
- OpenAI の立ち絵・Veo のコマ・手描きドットが混在している。
- ⚠**キャラとアイテムの手続き描画は禁止**という厳命があるため、Claude だけでは進められない。
  Gemini/Veo での生成は1本ずつ確認を仰ぐ運用。
